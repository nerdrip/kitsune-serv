'use strict';

const fs = require('fs');
const path = require('path');
const crypto = require('crypto');

const DEFAULT = {
  schemaVersion: 1, drives: [], offlineWorkspaces: [], searches: [], policies: [], forensicCases: [],
  secretLeases: [], canaries: [], collaborations: [], undoPoints: [], runbooks: [], teleports: [], schedules: [],
  receipts: [], cacheProfiles: [], eventStreams: [], trustRecords: [], stagingAreas: [], layouts: [], bookmarks: [],
  commandMatrices: [], transferQueues: [], editorPlans: [], sessionTimelines: [], filesystemSnapshots: [], syncProfiles: [], virtualFolders: [],
  detachedJobs: [], conflictCockpits: [], quarantines: [], terminalBranches: [], reviewInbox: [],
  executionPlans: [], sessionCanvases: [], adapterContracts: [], accessibilityProfiles: [],
  identityCenters: [], recoveryCapsules: [], reproducibilityRecords: [], dataPipelines: [], expertLayouts: [], shadowPlans: []
};
const FEATURES = Object.freeze([
  'remote-workspace-drive', 'delta-transfer-engine', 'offline-workspace', 'remote-code-intelligence',
  'universal-content-search', 'connection-graph', 'smart-jump-tunnel-manager', 'just-in-time-secrets',
  'policy-as-code-guardrails', 'forensic-mode', 'ephemeral-ssh-certificates', 'canary-operations',
  'production-safety-lens', 'digital-twin-sandbox', 'intent-terminal', 'collaborative-terminal',
  'terminal-heads-up-display', 'visual-permission-studio', 'remote-disaster-undo', 'living-runbooks',
  'adaptive-operations-scheduler', 'context-teleport', 'structured-output-canvas', 'file-relationship-graph',
  'signed-transfer-receipts', 'predictive-workspace-cache', 'live-remote-event-stream', 'semantic-file-history',
  'visual-batch-rename-transform', 'remote-data-inspector', 'connection-continuity', 'server-identity-trust-timeline',
  'reviewable-command-composer', 'terminal-focus-mode', 'universal-staging-area', 'multi-monitor-operations-layout',
  'command-file-bookmarks', 'explain-this-screen', 'remote-workspace-health', 'disposable-data-lens',
  'cross-server-command-matrix', 'transfer-control-center', 'direct-server-transfer', 'atomic-remote-editor',
  'session-time-machine', 'filesystem-snapshot-diff', 'smart-sync-profiles', 'archive-explorer',
  'remote-storage-fabric', 'live-virtual-folders', 'disk-space-visualizer', 'process-port-container-navigator',
  'secure-clipboard-bridge', 'shell-resurrection', 'terminal-file-manager-fusion',
  'execution-context-beacon', 'connection-waterfall-tuner', 'data-boundary-guard', 'live-output-privacy-shield',
  'detached-job-orchestrator', 'conflict-resolution-cockpit', 'executable-trust-inspector', 'resource-budgeted-commands',
  'remote-quarantine-lab', 'branchable-terminal', 'adaptive-ssh-channel-pool', 'terminal-files-review-inbox',
  'approved-execution-engine', 'visual-result-renderers', 'unified-remote-session-canvas', 'remote-desktop-quality-engine',
  'remote-desktop-file-bridge', 'capability-negotiator', 'optional-kitsune-agent', 'contextual-action-orb',
  'terminal-files-adapter-sdk', 'remote-operations-test-lab', 'keyboard-accessibility-pass', 'performance-large-fleet-pass',
  'semantic-shell-layer', 'config-studio', 'identity-trust-center', 'remote-desktop-pro',
  'live-process-network-explorer', 'workspace-recovery-capsule', 'cross-host-data-pipeline',
  'command-reproducibility', 'disposable-shadow-host', 'focus-incident-layouts'
]);

function clone(value) { return structuredClone(value); }
function read(file, fallback) { try { return JSON.parse(fs.readFileSync(file, 'utf8')); } catch { return clone(fallback); } }
function atomic(file, value) { fs.mkdirSync(path.dirname(file), { recursive: true }); const temp = `${file}.${process.pid}.tmp`; fs.writeFileSync(temp, JSON.stringify(value, null, 2), { mode: 0o600 }); try { fs.renameSync(temp, file); } catch (error) { if (!['EPERM', 'EEXIST'].includes(error.code)) throw error; fs.copyFileSync(temp, file); fs.unlinkSync(temp); } }
function clean(value, max = 2000) { return String(value ?? '').replace(/[\0\u202e\u2066-\u2069]/g, '').slice(0, max); }
function id(value, label = 'identifier') { const result = String(value || ''); if (!/^[A-Za-z0-9_.:@/-]{1,240}$/.test(result) || result.includes('..')) throw new Error(`Invalid ${label}`); return result; }
function remotePath(value) { const result = path.posix.normalize(String(value || '')); if (!result.startsWith('/') || result.includes('\0') || result.split('/').includes('..')) throw new Error('A safe absolute remote path is required'); return result; }
function digest(value) { return crypto.createHash('sha256').update(Buffer.isBuffer(value) ? value : String(value)).digest('hex'); }
function redact(value) { return clean(value, 2_000_000).replace(/(password|passwd|secret|token|api[_-]?key|authorization)(\s*[=:]\s*|\s+)([^\s;&|]+)/gi, '$1$2<redacted>').replace(/-----BEGIN (?:[A-Z0-9]+ )?PRIVATE KEY-----[\s\S]*?-----END (?:[A-Z0-9]+ )?PRIVATE KEY-----/g, '<private-key-redacted>'); }
function publicOnly(value) { if (Array.isArray(value)) return value.map(publicOnly); if (!value || typeof value !== 'object') return value; return Object.fromEntries(Object.entries(value).filter(([key]) => !/(pass|secret|token|private|credential|authorization|cookie)/i.test(key)).map(([key, item]) => [key, publicOnly(item)])); }
function derive(passphrase, salt) { return crypto.pbkdf2Sync(String(passphrase), salt, 310000, 32, 'sha256'); }
function seal(payload, passphrase) { if (String(passphrase).length < 12) throw new Error('Passphrase must contain at least 12 characters'); const salt = crypto.randomBytes(16); const iv = crypto.randomBytes(12); const cipher = crypto.createCipheriv('aes-256-gcm', derive(passphrase, salt), iv); const ciphertext = Buffer.concat([cipher.update(JSON.stringify(payload)), cipher.final()]); return { algorithm: 'aes-256-gcm', kdf: 'pbkdf2-sha256', iterations: 310000, salt: salt.toString('base64'), iv: iv.toString('base64'), tag: cipher.getAuthTag().toString('base64'), ciphertext: ciphertext.toString('base64') }; }
function openSealed(envelope, passphrase) { const decipher = crypto.createDecipheriv('aes-256-gcm', derive(passphrase, Buffer.from(envelope.salt, 'base64')), Buffer.from(envelope.iv, 'base64')); decipher.setAuthTag(Buffer.from(envelope.tag, 'base64')); return JSON.parse(Buffer.concat([decipher.update(Buffer.from(envelope.ciphertext, 'base64')), decipher.final()]).toString('utf8')); }
function now() { return new Date().toISOString(); }

class TerminalFileVisionManager {
  constructor(appRoot, dependencies = {}) {
    this.root = path.join(path.resolve(appRoot), 'terminal-file-vision');
    this.file = path.join(this.root, 'state.json');
    this.remote = dependencies.remoteAccess;
    this.ops = dependencies.remoteOperations;
    this.tunnels = dependencies.sshTunnel;
    this.advanced = dependencies.advanced;
    this.secretStore = dependencies.secretStore;
  }
  _state() { const state = read(this.file, DEFAULT); for (const [key, value] of Object.entries(DEFAULT)) if (!(key in state)) state[key] = clone(value); return state; }
  _save(state) { atomic(this.file, state); return state; }
  _key(name) { const keyName = `terminal-file-vision:${name}`; let key = this.secretStore?.get(keyName); if (!key) { key = crypto.randomBytes(32).toString('base64url'); this.secretStore?.set(keyName, key); } return key; }
  summary() { const state = this._state(); return { features: FEATURES.length, drives: state.drives.length, offlineWorkspaces: state.offlineWorkspaces.length, policies: state.policies.length, forensicCases: state.forensicCases.length, activeLeases: state.secretLeases.filter(item => !item.usedAt && new Date(item.expiresAt) > new Date()).length, collaborations: state.collaborations.length, undoPoints: state.undoPoints.length, runbooks: state.runbooks.length, schedules: state.schedules.length, receipts: state.receipts.length, eventStreams: state.eventStreams.length, trustedServers: state.trustRecords.length, stagingAreas: state.stagingAreas.length, bookmarks: state.bookmarks.length, pendingReviews: state.reviewInbox.filter(item => item.status === 'pending').length, detachedJobs: state.detachedJobs.length, quarantines: state.quarantines.length, approvedPlans: state.executionPlans.filter(item => item.status === 'approved').length, sessionCanvases: state.sessionCanvases.length, adapterContracts: state.adapterContracts.length, recoveryCapsules: state.recoveryCapsules.length, dataPipelines: state.dataPipelines.length, shadowPlans: state.shadowPlans.length }; }
  configuration() { const state = this._state(); return publicOnly({ ...state, secretLeases: state.secretLeases.map(item => ({ ...item, valueRef: undefined })), supportedFeatures: FEATURES }); }

  remoteWorkspaceDrive(input = {}) {
    const state = this._state(); const protocol = ['sftp', 'webdav', 's3', 'azure', 'local'].includes(input.protocol) ? input.protocol : 'sftp';
    const drive = { id: input.id ? id(input.id) : crypto.randomUUID(), name: clean(input.name || 'Remote Workspace', 120), sessionId: input.sessionId ? id(input.sessionId) : '', protocol, remoteRoot: protocol === 'local' ? path.resolve(clean(input.remoteRoot, 1000)) : remotePath(input.remoteRoot || '/'), localMount: path.resolve(clean(input.localMount || path.join(this.root, 'mounts', crypto.randomUUID()), 1000)), cache: { mode: ['metadata', 'on-demand', 'offline'].includes(input.cacheMode) ? input.cacheMode : 'on-demand', quotaBytes: Math.max(64 * 1024 * 1024, Math.min(100 * 1024 ** 3, Number(input.quotaBytes) || 2 * 1024 ** 3)) }, conflictStrategy: ['ask', 'keep-both', 'three-way'].includes(input.conflictStrategy) ? input.conflictStrategy : 'three-way', status: 'planned', requiresDriver: protocol !== 'local', updatedAt: now(), credentialsIncluded: false };
    state.drives = state.drives.filter(item => item.id !== drive.id); state.drives.push(drive); this._save(state);
    return { success: true, drive, plan: ['validate-identity', 'create-private-cache', 'mount-read-through', 'enable-write-journal', 'watch-conflicts'], executable: false };
  }

  deltaTransfer(input = {}) {
    const blockSize = Math.max(64 * 1024, Math.min(16 * 1024 * 1024, Number(input.blockSize) || 1024 * 1024));
    const source = (input.sourceBlocks || []).slice(0, 100000).map((value, index) => ({ index, hash: clean(typeof value === 'string' ? value : value.hash, 128), bytes: Number(value.bytes) || blockSize }));
    const target = new Set((input.targetHashes || []).map(value => clean(value, 128)));
    const missing = source.filter(item => !target.has(item.hash)); const totalBytes = source.reduce((sum, item) => sum + item.bytes, 0); const transferBytes = missing.reduce((sum, item) => sum + item.bytes, 0);
    return { algorithm: 'content-defined-block-plan', blockSize, totalBlocks: source.length, transferBlocks: missing, totalBytes, transferBytes, savedBytes: Math.max(0, totalBytes - transferBytes), compression: input.compression === false ? 'disabled' : 'adaptive-zstd', parallelStreams: Math.max(1, Math.min(16, Number(input.parallelStreams) || 4)), resumeJournal: true, verify: 'sha256', requiresApproval: true, executable: false };
  }

  offlineWorkspace(input = {}) {
    const state = this._state(); const action = input.action || 'create';
    if (action === 'create') { const item = { id: crypto.randomUUID(), name: clean(input.name || 'Offline workspace', 120), sessionId: id(input.sessionId), remoteRoot: remotePath(input.remoteRoot || '/'), localRoot: path.resolve(clean(input.localRoot || path.join(this.root, 'offline', crypto.randomUUID()), 1000)), baseRevision: clean(input.baseRevision || '', 128), status: 'offline-ready', conflictStrategy: 'three-way', createdAt: now() }; state.offlineWorkspaces.push(item); this._save(state); return { success: true, workspace: item, secretsCached: false }; }
    const item = state.offlineWorkspaces.find(value => value.id === input.id); if (!item) throw new Error('Unknown offline workspace');
    const base = String(input.baseContent || ''); const local = String(input.localContent || ''); const remote = String(input.remoteContent || ''); const conflict = local !== base && remote !== base && local !== remote;
    const merge = local === remote ? local : local === base ? remote : remote === base ? local : `<<<<<<< LOCAL\n${local}\n=======\n${remote}\n>>>>>>> REMOTE`;
    return { success: !conflict, workspaceId: item.id, conflict, merge, baseHash: digest(base), localHash: digest(local), remoteHash: digest(remote), requiresReview: conflict, executable: false };
  }

  codeIntelligence(input = {}) {
    const content = String(input.content || '').slice(0, 4 * 1024 * 1024); const language = clean(input.language || path.extname(input.path || '').slice(1) || 'text', 30).toLowerCase();
    const patterns = language.match(/^(js|jsx|ts|tsx|javascript|typescript)$/) ? /(?:function|class|const|let|var|interface|type)\s+([A-Za-z_$][\w$]*)/g : language === 'python' || language === 'py' ? /(?:def|class)\s+([A-Za-z_]\w*)/g : /(?:function|class|struct|enum|interface)\s+([A-Za-z_]\w*)/g;
    const symbols = [...content.matchAll(patterns)].slice(0, 5000).map(match => ({ name: match[1], line: content.slice(0, match.index).split('\n').length, kind: match[0].split(/\s/)[0] }));
    const diagnostics = []; content.split(/\r?\n/).forEach((line, index) => { if (line.length > 240) diagnostics.push({ line: index + 1, severity: 'hint', message: 'Very long line' }); if (/\b(TODO|FIXME)\b/.test(line)) diagnostics.push({ line: index + 1, severity: 'info', message: 'Unresolved work marker' }); });
    return { language, path: clean(input.path, 1000), symbols, diagnostics: diagnostics.slice(0, 1000), outlineOnly: true, serverCodeExecuted: false, bytesAnalyzed: Buffer.byteLength(content) };
  }

  universalSearch(input = {}) {
    const query = clean(input.query, 500).toLocaleLowerCase(); if (!query) throw new Error('Search query is required');
    const records = (input.records || []).slice(0, 200000).map(publicOnly); const filters = input.filters || {};
    const results = records.filter(item => `${item.name || ''}\n${item.path || ''}\n${item.content || ''}`.toLocaleLowerCase().includes(query)).filter(item => !filters.sessionId || item.sessionId === filters.sessionId).filter(item => !filters.owner || item.owner === filters.owner).filter(item => !filters.gitState || item.gitState === filters.gitState).slice(0, Math.max(1, Math.min(2000, Number(input.limit) || 200)));
    return { query, results, searched: records.length, truncated: results.length >= (Number(input.limit) || 200), indexRemainsLocal: true, querySentToServers: false };
  }

  connectionGraph(input = {}) {
    const sessions = (input.sessions || []).slice(0, 1000).map(publicOnly); const tunnels = (input.tunnels || []).slice(0, 2000).map(publicOnly); const nodes = [{ id: 'local', kind: 'workstation', label: 'This computer', health: 'ready' }]; const edges = [];
    for (const session of sessions) { const nodeId = `server:${id(session.id)}`; nodes.push({ id: nodeId, kind: 'server', label: clean(session.name || session.host, 120), environment: clean(session.environment || 'unknown', 30), health: clean(session.health || 'unknown', 30) }); const via = session.jumpHostId ? `server:${id(session.jumpHostId)}` : 'local'; edges.push({ from: via, to: nodeId, kind: session.jumpHostId ? 'jump' : 'ssh', encrypted: true }); }
    for (const tunnel of tunnels) { const tunnelId = `tunnel:${id(tunnel.id)}`; nodes.push({ id: tunnelId, kind: 'tunnel', label: `${tunnel.localPort || '?'} → ${clean(tunnel.remoteHost, 120)}:${tunnel.remotePort || '?'}`, health: clean(tunnel.status || 'planned', 30) }); edges.push({ from: `server:${id(tunnel.sessionId)}`, to: tunnelId, kind: 'forward' }); }
    return { nodes, edges, roots: ['local'], layout: 'layered', cycles: edges.filter(edge => edge.from === edge.to), generatedAt: now() };
  }

  smartTunnel(input = {}) {
    const hops = (input.hops || []).slice(0, 8).map(value => ({ sessionId: id(value.sessionId), host: clean(value.host, 253), port: Math.max(1, Math.min(65535, Number(value.port) || 22)), hostKeyRequired: true }));
    if (!hops.length && !input.sessionId) throw new Error('At least one server or jump host is required'); const localPort = Math.max(1, Math.min(65535, Number(input.localPort) || 8080));
    return { mode: ['local', 'remote', 'dynamic'].includes(input.mode) ? input.mode : 'local', sessionId: input.sessionId ? id(input.sessionId) : hops.at(-1).sessionId, hops, localPort, remoteHost: clean(input.remoteHost || '127.0.0.1', 253), remotePort: Math.max(1, Math.min(65535, Number(input.remotePort) || 80)), conflictCheck: { required: true, localPort }, reconnect: { enabled: true, maxBackoffSeconds: 60 }, requiresApproval: true, executable: false };
  }

  justInTimeSecret(input = {}) {
    const state = this._state(); if (input.action === 'consume') { const lease = state.secretLeases.find(item => item.id === input.id); if (!lease || lease.usedAt || new Date(lease.expiresAt) <= new Date() || (lease.sessionId && lease.sessionId !== input.sessionId)) throw new Error('Secret lease is unavailable'); const value = this.secretStore.get(lease.valueRef); lease.usedAt = now(); this.secretStore.remove(lease.valueRef); this._save(state); return { success: true, value, environmentName: lease.environmentName, expiresAt: lease.expiresAt, oneUse: true }; }
    const leaseId = crypto.randomUUID(); const ttl = Math.max(5, Math.min(900, Number(input.ttlSeconds) || 60)); const valueRef = `terminal-file-vision:lease:${leaseId}`; this.secretStore.set(valueRef, String(input.value || '')); const lease = { id: leaseId, label: clean(input.label || 'JIT secret', 120), environmentName: clean(input.environmentName || 'KITSUNE_SECRET', 80).replace(/[^A-Za-z0-9_]/g, '_'), sessionId: input.sessionId ? id(input.sessionId) : '', valueRef, expiresAt: new Date(Date.now() + ttl * 1000).toISOString(), usedAt: '', createdAt: now() }; state.secretLeases.push(lease); this._save(state); return { success: true, lease: publicOnly({ ...lease, valueRef: undefined }), injection: 'process-environment-only', persistedInTerminal: false, oneUse: true };
  }

  policyGuardrails(input = {}) {
    const state = this._state(); if (input.action === 'save') { const policy = { id: crypto.randomUUID(), name: clean(input.name || 'Operations policy', 120), environment: clean(input.environment || '*', 30), readOnly: Boolean(input.readOnly), forbiddenCommands: (input.forbiddenCommands || []).map(value => clean(value, 120)).slice(0, 100), protectedPaths: (input.protectedPaths || []).map(remotePath).slice(0, 100), requireSecondPerson: Boolean(input.requireSecondPerson), createdAt: now() }; state.policies.push(policy); this._save(state); return { success: true, policy }; }
    const command = String(input.command || ''); const targetPath = input.path ? remotePath(input.path) : ''; const matches = state.policies.filter(policy => policy.environment === '*' || policy.environment === input.environment); const reasons = []; for (const policy of matches) { if (policy.readOnly && /\b(rm|mv|cp|chmod|chown|sed\s+-i|tee|truncate|dd|mkfs|systemctl\s+(restart|stop)|kubectl\s+(apply|delete))\b/i.test(command)) reasons.push(`${policy.name}: read-only environment`); for (const word of policy.forbiddenCommands) if (command.toLowerCase().includes(word.toLowerCase())) reasons.push(`${policy.name}: forbidden command ${word}`); for (const protectedPath of policy.protectedPaths) if (targetPath === protectedPath || targetPath.startsWith(`${protectedPath}/`)) reasons.push(`${policy.name}: protected path ${protectedPath}`); if (policy.requireSecondPerson && !input.secondApproval) reasons.push(`${policy.name}: second approval required`); }
    return { allowed: reasons.length === 0, reasons, matchedPolicies: matches.map(policy => policy.id), environment: clean(input.environment || 'unknown', 30), commandHash: digest(command), commandPersisted: false };
  }

  forensicMode(input = {}) {
    const state = this._state(); const evidence = (input.evidence || []).slice(0, 10000).map(item => ({ path: clean(item.path, 1000), sha256: item.sha256 || digest(item.content || ''), bytes: Number(item.bytes) || Buffer.byteLength(String(item.content || '')), collectedAt: item.collectedAt || now() })); const body = { id: crypto.randomUUID(), name: clean(input.name || 'Forensic case', 120), sessionId: input.sessionId ? id(input.sessionId) : '', evidence, readOnly: true, serverMutations: false, createdAt: now() }; const signature = crypto.createHmac('sha256', this._key('forensics-signing')).update(JSON.stringify(body)).digest('hex'); const record = { ...body, signature, chainHash: digest(evidence.map(item => item.sha256).join(':')) }; state.forensicCases.push(record); this._save(state); return { success: true, case: record, exportFormat: 'signed-json', verification: 'hmac-sha256' };
  }

  ephemeralSshCertificate(input = {}) { const minutes = Math.max(1, Math.min(480, Number(input.minutes) || 30)); return { principal: clean(input.principal, 120), sessionId: id(input.sessionId), publicKeyHash: digest(input.publicKey || ''), validAfter: now(), validBefore: new Date(Date.now() + minutes * 60000).toISOString(), keySource: ['windows-hello', 'fido2', 'ssh-agent'].includes(input.keySource) ? input.keySource : 'ssh-agent', caRequest: { algorithm: 'ssh-ed25519-cert-v01@openssh.com', serial: crypto.randomBytes(8).toString('hex') }, privateKeyExported: false, requiresCaApproval: true, executable: false };
  }

  canaryOperation(input = {}) { const state = this._state(); const targets = [...new Set((input.sessionIds || []).map(value => id(value)))].slice(0, 5000); if (!targets.length) throw new Error('At least one target is required'); const canaryCount = Math.max(1, Math.min(targets.length, Number(input.canaryCount) || 1)); const plan = { id: crypto.randomUUID(), name: clean(input.name || 'Canary operation', 120), operationHash: digest(input.operation || ''), operationPreview: redact(input.operation || '').slice(0, 500), canary: targets.slice(0, canaryCount), remainder: targets.slice(canaryCount), gates: ['exit-code', 'health-check', 'error-budget', 'manual-approval'], status: 'planned', createdAt: now(), executable: false }; state.canaries.push(plan); this._save(state); return { success: true, plan };
  }

  productionSafety(input = {}) { const environment = clean(input.environment || 'unknown', 30).toLowerCase(); const production = /^(prod|production|live)$/.test(environment); const command = String(input.command || ''); const destructive = /\b(rm|delete|drop|truncate|reboot|shutdown|mkfs|format|systemctl\s+(stop|restart)|kubectl\s+delete)\b/i.test(command); return { environment, production, theme: production ? 'production-red' : environment.match(/stag/) ? 'staging-amber' : 'standard', banner: production ? 'PRODUCTION · CHANGES AFFECT LIVE USERS' : environment.toUpperCase(), requiresTypedConfirmation: production && destructive, confirmationPhrase: production && destructive ? clean(input.serverName || 'PRODUCTION', 120) : '', readOnlyRecommended: production, commandHash: digest(command), commandPersisted: false };
  }

  digitalTwin(input = {}) { const command = redact(input.command || ''); const effects = []; const rules = [[/\brm\b/i, 'delete-files'], [/\b(mv|rename)\b/i, 'move-files'], [/\b(chmod|chown|setfacl)\b/i, 'change-permissions'], [/\b(systemctl|service)\s+(restart|stop|start)/i, 'change-service-state'], [/\b(apt|dnf|yum|pacman)\b/i, 'change-packages'], [/\b(kubectl|docker)\b/i, 'change-workloads'], [/>|tee|sed\s+-i/i, 'write-files']]; for (const [pattern, kind] of rules) if (pattern.test(command)) effects.push(kind); const confidence = effects.length ? Math.min(0.95, 0.55 + effects.length * 0.08) : 0.35; return { command, model: { filesystemRevision: clean(input.filesystemRevision, 128), packageRevision: clean(input.packageRevision, 128), serviceRevision: clean(input.serviceRevision, 128) }, predictedEffects: effects, confidence, unknownEffects: confidence < 0.8, serverExecuted: false, sandbox: 'local-metadata-twin', requiresRealPreflight: true };
  }

  intentTerminal(input = {}) { const intent = clean(input.intent, 2000); const patterns = [{ re: /(show|list|pokaż).*(disk|dysk)/i, commands: ['df -h', 'du -x -d 1 / 2>/dev/null | sort -n'] }, { re: /(logs?|logi).*(service|usług)/i, commands: [`journalctl -u ${clean(input.unit || 'SERVICE', 120)} -n 200 --no-pager`] }, { re: /(find|znajdź).*(large|duż).*(files?|pliki)/i, commands: ['find / -xdev -type f -size +1G -printf "%s\\t%p\\n" 2>/dev/null | sort -nr | head -n 100'] }, { re: /(ports?|porty|listening)/i, commands: ['ss -lntup'] }]; const match = patterns.find(item => item.re.test(intent)); return { intent, understood: Boolean(match), steps: (match?.commands || []).map((command, index) => ({ id: index + 1, command, risk: 'read-only', approved: false })), targetSessionIds: (input.sessionIds || []).map(value => id(value)).slice(0, 100), mutationsExecuted: false, requiresApproval: true, explanation: match ? 'Matched a deterministic local intent template.' : 'No deterministic template matched; no command was generated.' };
  }

  collaboration(input = {}) { const state = this._state(); if (input.action === 'create') { const item = { id: crypto.randomUUID(), name: clean(input.name || 'Collaborative terminal', 120), resource: clean(input.resource || 'terminal', 240), members: (input.members || []).slice(0, 50).map(member => ({ name: clean(member.name, 120), role: ['observer', 'operator', 'approver'].includes(member.role) ? member.role : 'observer' })), proposals: [], keyboardOwner: clean(input.keyboardOwner || '', 120), recording: true, expiresAt: new Date(Date.now() + Math.max(5, Math.min(480, Number(input.minutes) || 120)) * 60000).toISOString(), createdAt: now() }; state.collaborations.push(item); this._save(state); return { success: true, collaboration: item }; }
    const item = state.collaborations.find(value => value.id === input.id); if (!item) throw new Error('Unknown collaboration'); const proposal = { id: crypto.randomUUID(), author: clean(input.author, 120), command: redact(input.command), commandHash: digest(input.command || ''), approvals: (input.approvals || []).map(value => clean(value, 120)).slice(0, 20), status: 'pending', createdAt: now() }; proposal.status = proposal.approvals.length >= (input.requiredApprovals || 1) ? 'approved' : 'pending'; item.proposals.push(proposal); this._save(state); return { success: true, proposal, executable: proposal.status === 'approved' && Boolean(input.executeApproved) };
  }

  terminalHud(input = {}) { const before = input.before || {}; const after = input.after || {}; const changed = (name, unit = '') => ({ metric: name, before: Number(before[name]) || 0, after: Number(after[name]) || 0, delta: (Number(after[name]) || 0) - (Number(before[name]) || 0), unit }); return { commandHash: digest(input.command || ''), metrics: [changed('cpuPercent', '%'), changed('memoryBytes', 'bytes'), changed('processes'), changed('connections'), changed('diskBytes', 'bytes')], files: (input.files || []).map(value => clean(value, 1000)).slice(0, 500), processes: (input.processes || []).map(publicOnly).slice(0, 500), connections: (input.connections || []).map(publicOnly).slice(0, 500), sampledAt: now(), commandPersisted: false };
  }

  permissionStudio(input = {}) { const target = remotePath(input.path); const current = Math.max(0, Math.min(0o7777, Number.parseInt(String(input.currentMode || '644'), 8))); const proposed = Math.max(0, Math.min(0o7777, Number.parseInt(String(input.proposedMode || '644'), 8))); const bits = mode => ({ owner: { read: Boolean(mode & 0o400), write: Boolean(mode & 0o200), execute: Boolean(mode & 0o100) }, group: { read: Boolean(mode & 0o040), write: Boolean(mode & 0o020), execute: Boolean(mode & 0o010) }, others: { read: Boolean(mode & 0o004), write: Boolean(mode & 0o002), execute: Boolean(mode & 0o001) } }); const risk = (proposed & 0o002) ? 'critical' : (proposed & 0o020) || (proposed & 0o4000) ? 'high' : 'normal'; return { path: target, current: current.toString(8).padStart(3, '0'), proposed: proposed.toString(8).padStart(3, '0'), matrix: bits(proposed), acl: (input.acl || []).map(publicOnly).slice(0, 500), selinux: clean(input.selinux || '', 240), effectiveAccess: (input.identities || []).slice(0, 100).map(identity => { const identityClass = ['owner', 'group', 'others'].includes(identity.class) ? identity.class : 'others'; return { identity: clean(identity.name, 120), class: identityClass, ...bits(proposed)[identityClass] }; }), risk, command: `chmod ${proposed.toString(8)} -- '${target.replace(/'/g, `'"'"'`)}'`, requiresApproval: true, executable: false };
  }

  disasterUndo(input = {}) { const state = this._state(); const files = (input.files || []).slice(0, 1000).map(item => ({ path: remotePath(item.path), sha256: item.sha256 || digest(item.content || ''), mode: clean(item.mode || '', 8), owner: clean(item.owner || '', 120), bytes: Number(item.bytes) || Buffer.byteLength(String(item.content || '')) })); const point = { id: crypto.randomUUID(), name: clean(input.name || 'Pre-operation undo point', 120), sessionId: id(input.sessionId), operationHash: digest(input.operation || ''), files, serviceStates: (input.serviceStates || []).map(publicOnly).slice(0, 500), restoreOrder: ['files', 'permissions', 'owners', 'services', 'health-check'], expiresAt: new Date(Date.now() + Math.max(1, Math.min(168, Number(input.hours) || 24)) * 3600000).toISOString(), createdAt: now(), minimalScope: true }; state.undoPoints.push(point); this._save(state); return { success: true, undoPoint: point, requiresApprovalToRestore: true };
  }

  livingRunbook(input = {}) { const state = this._state(); const commands = (input.commands || []).slice(0, 200).map(command => redact(typeof command === 'string' ? command : command.command)); const steps = commands.map((command, index) => ({ id: index + 1, name: clean(input.stepNames?.[index] || `Step ${index + 1}`, 120), command, commandHash: digest(command), parameters: [...command.matchAll(/\$\{([A-Z][A-Z0-9_]*)\}/g)].map(match => match[1]), precondition: clean(input.preconditions?.[index] || 'previous-step-success', 240), verification: clean(input.verifications?.[index] || 'exit-code-zero', 240), rollback: redact(input.rollbacks?.[index] || ''), approval: index === 0 || /\b(rm|delete|restart|deploy|apply)\b/i.test(command) })); const runbook = { id: crypto.randomUUID(), name: clean(input.name || 'Living runbook', 120), steps, sourceSessionId: input.sessionId ? id(input.sessionId) : '', secretsIncluded: false, status: 'draft', createdAt: now() }; state.runbooks.push(runbook); this._save(state); return { success: true, runbook };
  }

  adaptiveScheduler(input = {}) { const state = this._state(); const jobs = (input.jobs || []).slice(0, 1000).map((job, index) => { const load = Math.max(0, Math.min(100, Number(job.serverLoad) || 0)); const network = Math.max(0, Math.min(100, Number(job.networkQuality) || 50)); const urgency = Math.max(0, Math.min(100, Number(job.urgency) || 50)); const cost = Math.max(0, Math.min(100, Number(job.cloudCost) || 0)); const score = Math.round(urgency * 0.45 + network * 0.25 + (100 - load) * 0.2 + (100 - cost) * 0.1); return { id: job.id ? id(job.id) : `job-${index + 1}`, kind: clean(job.kind || 'transfer', 40), score, throttlePercent: load > 80 ? 20 : load > 60 ? 50 : 100, startAfter: clean(job.windowStart || now(), 40), reason: load > 80 ? 'deferred/high-load' : network < 30 ? 'throttled/poor-network' : 'ready' }; }).sort((a, b) => b.score - a.score); const schedule = { id: crypto.randomUUID(), jobs, policy: 'cost-load-network-aware', createdAt: now(), executable: false }; state.schedules.push(schedule); this._save(state); return { success: true, schedule };
  }

  contextTeleport(input = {}) { const requestedTarget = clean(input.target, 1000).trim(); if (!requestedTarget) throw new Error('Output target is required'); const target = path.resolve(requestedTarget); const payload = publicOnly({ schemaVersion: 1, workspace: input.workspace || {}, tabs: input.tabs || [], tunnels: input.tunnels || [], checkpoints: input.checkpoints || [], serverProfiles: input.serverProfiles || [], createdAt: now() }); payload.secretsIncluded = false; const envelope = seal(payload, input.passphrase); fs.mkdirSync(path.dirname(target), { recursive: true }); fs.writeFileSync(target, JSON.stringify(envelope, null, 2), { mode: 0o600 }); const state = this._state(); state.teleports.push({ id: crypto.randomUUID(), target, sha256: digest(fs.readFileSync(target)), bytes: fs.statSync(target).size, createdAt: now(), secretsIncluded: false }); this._save(state); return { success: true, target, sha256: state.teleports.at(-1).sha256, secretsIncluded: false }; }
  openTeleport(target, passphrase) { return { success: true, payload: openSealed(JSON.parse(fs.readFileSync(path.resolve(target), 'utf8')), passphrase) }; }

  structuredOutput(input = {}) {
    const raw = clean(input.output, 4 * 1024 * 1024); const trimmed = raw.trim(); let kind = 'text'; let rows = []; let tree = null;
    try { tree = JSON.parse(trimmed); kind = 'json'; rows = Array.isArray(tree) ? tree.slice(0, 5000).map(publicOnly) : [publicOnly(tree)]; } catch {}
    if (kind === 'text') { const lines = trimmed.split(/\r?\n/).slice(0, 10000); const delimiter = lines[0]?.includes('\t') ? '\t' : lines[0]?.includes(',') ? ',' : ''; if (delimiter && lines.length > 1) { const headers = lines[0].split(delimiter).map(value => clean(value.trim(), 120)); const candidate = lines.slice(1).map(line => line.split(delimiter)); if (candidate.every(cells => cells.length === headers.length)) { kind = delimiter === '\t' ? 'table' : 'csv'; rows = candidate.slice(0, 5000).map(cells => Object.fromEntries(headers.map((header, index) => [header || `column${index + 1}`, clean(cells[index], 4000)]))); } } if (!rows.length && lines.some(line => /(^|\s)(trace|debug|info|warn|warning|error|fatal|failed)(\s|:|$)/i.test(line))) { kind = 'log'; rows = lines.map((line, index) => ({ line: index + 1, level: /\b(error|fatal|failed)\b/i.test(line) ? 'error' : /\bwarn/i.test(line) ? 'warning' : 'info', message: line })); } }
    return { kind, rawAvailable: true, bytes: Buffer.byteLength(raw), rows, tree, views: kind === 'json' ? ['tree', 'cards', 'raw'] : rows.length ? ['cards', 'table', 'raw'] : ['raw'], truncated: Buffer.byteLength(String(input.output || '')) > 4 * 1024 * 1024, actionsExecutable: false };
  }

  fileRelationshipGraph(input = {}) {
    const files = (input.files || []).slice(0, 5000).map(file => ({ path: remotePath(file.path), content: String(file.content || '').slice(0, 2 * 1024 * 1024), kind: clean(file.kind || path.posix.extname(file.path).slice(1) || 'file', 40) })); const nodes = files.map(file => ({ id: file.path, label: path.posix.basename(file.path), kind: file.kind })); const known = new Set(files.map(file => file.path)); const edges = [];
    for (const file of files) { const base = path.posix.dirname(file.path); const dependencies = [...file.content.matchAll(/(?:import\s+(?:[^'";]+\s+from\s+)?|require\s*\(|include\s+|include:|env_file:\s*|EnvironmentFile=|proxy_pass\s+)["']?([^\s'";)]+)/g)].map(match => match[1]); for (const dependency of dependencies.slice(0, 1000)) { let target = dependency.startsWith('/') ? path.posix.normalize(dependency) : path.posix.normalize(path.posix.join(base, dependency)); if (!path.posix.extname(target)) for (const extension of ['.js', '.ts', '.json', '.yml', '.yaml']) if (known.has(`${target}${extension}`)) { target += extension; break; } if (!known.has(target)) nodes.push({ id: target, label: path.posix.basename(target), kind: 'external', unresolved: true }); edges.push({ from: file.path, to: target, kind: 'references', resolved: known.has(target) }); } }
    return { nodes: [...new Map(nodes.map(node => [node.id, node])).values()].slice(0, 10000), edges: edges.slice(0, 20000), layout: 'dependency', sourceExecuted: false };
  }

  signedTransferReceipt(input = {}) {
    const state = this._state(); const providedHash = clean(input.sha256, 64); if (providedHash && !/^[a-f0-9]{64}$/i.test(providedHash)) throw new Error('Transfer receipt requires a valid SHA-256'); const body = { id: crypto.randomUUID(), source: publicOnly(input.source || {}), destination: publicOnly(input.destination || {}), sha256: providedHash || digest(input.content || ''), bytes: Math.max(0, Number(input.bytes) || Buffer.byteLength(String(input.content || ''))), operator: clean(input.operator || 'Local owner', 120), validation: clean(input.validation || 'sha256-verified', 80), completedAt: input.completedAt || now(), credentialsIncluded: false }; const signature = crypto.createHmac('sha256', this._key('receipt-signing')).update(JSON.stringify(body)).digest('hex'); const receipt = { ...body, signature, signatureAlgorithm: 'hmac-sha256' }; state.receipts.push(receipt); this._save(state); return { success: true, receipt };
  }

  predictiveCache(input = {}) {
    const state = this._state(); const events = (input.events || []).slice(0, 100000).map(item => ({ path: remotePath(item.path), at: item.at || now(), bytes: Math.max(0, Number(item.bytes) || 0), contentRequested: Boolean(item.contentRequested) })); const scores = new Map(); for (const event of events) { const ageHours = Math.max(0, (Date.now() - new Date(event.at).getTime()) / 3600000); const weight = (event.contentRequested ? 2 : 1) / (1 + ageHours / 24); scores.set(event.path, (scores.get(event.path) || 0) + weight); } const candidates = [...scores].sort((a, b) => b[1] - a[1]).slice(0, Math.max(1, Math.min(500, Number(input.limit) || 50))).map(([path, score]) => ({ path, score: Number(score.toFixed(3)), prefetch: events.find(item => item.path === path)?.bytes <= 256 * 1024 ? 'small-content' : 'metadata-only' })); const profile = { id: crypto.randomUUID(), sessionId: input.sessionId ? id(input.sessionId) : '', candidates, localLearningOnly: true, contentLimitBytes: 256 * 1024, createdAt: now() }; state.cacheProfiles.push(profile); this._save(state); return { success: true, profile };
  }

  liveEventStream(input = {}) {
    const state = this._state(); const platform = ['linux', 'windows', 'macos'].includes(input.platform) ? input.platform : 'linux'; const provider = platform === 'windows' ? 'usn-journal' : platform === 'macos' ? 'fsevents' : 'inotify'; const events = (input.events || []).slice(0, 50000).map((event, index) => ({ sequence: index + 1, kind: ['create', 'modify', 'delete', 'rename', 'chmod'].includes(event.kind) ? event.kind : 'modify', path: remotePath(event.path), oldPath: event.oldPath ? remotePath(event.oldPath) : '', at: event.at || now(), process: clean(event.process, 120), pid: Math.max(0, Number(event.pid) || 0) })); const stream = { id: crypto.randomUUID(), sessionId: id(input.sessionId), root: remotePath(input.root || '/'), platform, provider, events, cursor: clean(input.cursor || digest(events.length).slice(0, 16), 128), incrementalRefresh: true, createdAt: now() }; state.eventStreams.push(stream); this._save(state); return { success: true, stream };
  }

  semanticHistory(input = {}) {
    const beforeRaw = String(input.before || '').slice(0, 4 * 1024 * 1024); const afterRaw = String(input.after || '').slice(0, 4 * 1024 * 1024); let before; let after; try { before = JSON.parse(beforeRaw); after = JSON.parse(afterRaw); } catch { const parse = value => Object.fromEntries(value.split(/\r?\n/).map(line => line.match(/^\s*([A-Za-z0-9_.-]+)\s*[:=]\s*(.*?)\s*$/)).filter(Boolean).map(match => [match[1], match[2]])); before = parse(beforeRaw); after = parse(afterRaw); }
    const flatten = (value, prefix = '', result = {}) => { if (value && typeof value === 'object' && !Array.isArray(value)) for (const [key, item] of Object.entries(value)) flatten(item, prefix ? `${prefix}.${key}` : key, result); else result[prefix] = value; return result; }; const left = flatten(before); const right = flatten(after); const keys = [...new Set([...Object.keys(left), ...Object.keys(right)])]; const changes = keys.filter(key => JSON.stringify(left[key]) !== JSON.stringify(right[key])).slice(0, 10000).map(key => { const kind = !(key in left) ? 'added' : !(key in right) ? 'removed' : 'changed'; const sensitive = /(pass|secret|token|private|credential|authorization|cookie|api[_-]?key)/i.test(key); return { path: key, kind, before: sensitive ? '<redacted>' : publicOnly(left[key]), after: sensitive ? '<redacted>' : publicOnly(right[key]), explanation: sensitive ? `${kind[0].toUpperCase()}${kind.slice(1)} sensitive field ${key}; values redacted` : kind === 'added' ? `Added ${key}` : kind === 'removed' ? `Removed ${key}` : `Changed ${key} from ${JSON.stringify(left[key])} to ${JSON.stringify(right[key])}` }; }); return { path: clean(input.path, 1000), changes, beforeHash: digest(beforeRaw), afterHash: digest(afterRaw), parser: beforeRaw.trim().startsWith('{') ? 'json' : 'key-value', executable: false };
  }

  batchTransform(input = {}) {
    const files = (input.files || []).slice(0, 10000).map(item => ({ path: remotePath(item.path), content: item.content == null ? undefined : String(item.content).slice(0, 4 * 1024 * 1024) })); const prefix = clean(input.prefix, 120); const suffix = clean(input.suffix, 120); const find = clean(input.find, 240); const replace = clean(input.replace, 240); if ([prefix, suffix, find, replace].some(value => /[\\/]/.test(value))) throw new Error('Batch rename rules cannot contain path separators'); const entries = files.map(file => { const directory = path.posix.dirname(file.path); const extension = path.posix.extname(file.path); const base = path.posix.basename(file.path, extension); const renamed = `${prefix}${find ? base.split(find).join(replace) : base}${suffix}${extension}`; let transformed = file.content; if (transformed != null && input.lineEndings === 'lf') transformed = transformed.replace(/\r\n/g, '\n'); if (transformed != null && input.lineEndings === 'crlf') transformed = transformed.replace(/\r?\n/g, '\r\n'); return { before: file.path, after: path.posix.join(directory, renamed), renamed: file.path !== path.posix.join(directory, renamed), contentHashBefore: file.content == null ? '' : digest(file.content), contentHashAfter: transformed == null ? '' : digest(transformed), contentChanged: transformed !== file.content }; }); const collisions = entries.filter((entry, index) => entries.findIndex(other => other.after === entry.after) !== index).map(entry => entry.after); return { entries, collisions: [...new Set(collisions)], applicable: collisions.length === 0, requiresApproval: true, executable: false, rollback: 'reverse-rename-and-content-snapshot' };
  }

  dataInspector(input = {}) {
    const format = ['csv', 'jsonl', 'json', 'sqlite', 'parquet'].includes(input.format) ? input.format : 'jsonl'; const content = String(input.content || '').slice(0, 8 * 1024 * 1024); let rows = []; let columns = [];
    if (format === 'json') { const value = JSON.parse(content || '[]'); rows = (Array.isArray(value) ? value : [value]).map(publicOnly); } else if (format === 'jsonl') rows = content.split(/\r?\n/).filter(Boolean).slice(0, 10000).map(line => publicOnly(JSON.parse(line))); else if (format === 'csv') { const lines = content.split(/\r?\n/).filter(Boolean); columns = (lines.shift() || '').split(',').map(value => clean(value.trim(), 120)); rows = lines.slice(0, 10000).map(line => Object.fromEntries(line.split(',').map((value, index) => [columns[index] || `column${index + 1}`, clean(value, 4000)]))); } else { columns = (input.schema || []).map(value => clean(value, 120)).slice(0, 1000); rows = (input.rows || []).map(publicOnly).slice(0, 10000); }
    if (!columns.length) columns = [...new Set(rows.flatMap(row => row && typeof row === 'object' ? Object.keys(row) : []))].slice(0, 1000); const pageSize = Math.max(1, Math.min(500, Number(input.pageSize) || 100)); const page = Math.max(1, Number(input.page) || 1); return { format, columns, rows: rows.slice((page - 1) * pageSize, page * pageSize), page, pageSize, totalLoadedRows: rows.length, boundedRead: true, fullFileDownloaded: false, mutationsAllowed: false };
  }

  connectionContinuity(input = {}) {
    const options = [{ method: 'tmux-ssh', available: Boolean(input.tmux), score: 70 }, { method: 'mosh', available: Boolean(input.mosh), score: 95 }, { method: 'agent-relay', available: Boolean(input.agent && input.relay), score: 90 }, { method: 'ssh-reconnect', available: true, score: 55 }]; if (input.highLatency) for (const option of options) if (option.method === 'mosh') option.score += 5; const selected = options.filter(item => item.available).sort((a, b) => b.score - a.score)[0]; return { sessionId: id(input.sessionId), selected: selected.method, candidates: options, preserved: ['cwd', 'tmux', 'tabs', 'transfer-journal', 'tunnels'], transition: ['freeze-input', 'checkpoint-context', 'reconnect-transport', 'verify-host-identity', 'resume-session'], requiresIdentityRecheck: true, executable: false };
  }

  trustTimeline(input = {}) {
    const state = this._state(); const sessionId = id(input.sessionId); const fingerprint = clean(input.fingerprint, 256); if (!fingerprint) throw new Error('Server fingerprint is required'); const previous = [...state.trustRecords].reverse().find(item => item.sessionId === sessionId); const status = !previous ? 'first-seen' : previous.fingerprint === fingerprint ? 'trusted' : 'changed'; const record = { id: crypto.randomUUID(), sessionId, fingerprint, algorithm: clean(input.algorithm || 'ssh-ed25519', 80), host: clean(input.host, 253), addresses: (input.addresses || []).map(value => clean(value, 64)).slice(0, 50), dns: clean(input.dns, 253), certificateExpiresAt: clean(input.certificateExpiresAt, 40), status, previousFingerprint: previous?.fingerprint || '', observedAt: now() }; state.trustRecords.push(record); this._save(state); return { success: status !== 'changed', blocked: status === 'changed', record, visualDiff: previous ? { fingerprintChanged: previous.fingerprint !== fingerprint, addressesAdded: record.addresses.filter(value => !previous.addresses.includes(value)), dnsChanged: previous.dns !== record.dns } : null, requiresManualTrust: status !== 'trusted' };
  }

  commandComposer(input = {}) {
    const allowed = new Set(['command', 'filter', 'sort', 'limit', 'redirect']); const stages = (input.stages || []).slice(0, 30).map((stage, index) => { const kind = clean(stage.kind, 30); if (!allowed.has(kind)) throw new Error(`Unsupported command stage: ${kind}`); const value = redact(stage.value).replace(/[\r\n]/g, ' ').trim(); if (!value) throw new Error(`Stage ${index + 1} is empty`); return { id: index + 1, kind, value, input: index === 0 ? 'terminal' : index, output: index + 2, risk: kind === 'redirect' ? 'write' : 'read' }; }); if (!stages.length || stages[0].kind !== 'command') throw new Error('Composer must start with a command stage'); const separators = { filter: ' | ', sort: ' | ', limit: ' | ', redirect: ' > ' }; let preview = stages[0].value; for (const stage of stages.slice(1)) preview += `${separators[stage.kind] || ' '}${stage.value}`; return { stages, preview, risk: stages.some(stage => stage.risk === 'write') ? 'write' : 'read-only', requiresApproval: stages.some(stage => stage.risk === 'write'), executable: false, dataFlow: stages.map(stage => ({ from: stage.input, to: stage.output })) };
  }

  focusMode(input = {}) { const environment = clean(input.environment || 'unknown', 30); return { sessionId: input.sessionId ? id(input.sessionId) : '', serverName: clean(input.serverName || 'Local terminal', 120), environment, theme: /prod/i.test(environment) ? 'production-red' : 'focus-dark', visible: ['environment-banner', 'terminal', 'risk-indicator', 'exit-focus'], hidden: ['sidebar', 'secondary-tools', 'notifications', 'unrelated-tabs'], fontScale: Math.max(1, Math.min(1.8, Number(input.fontScale) || 1.2)), readOnly: Boolean(input.readOnly), ephemeral: true }; }

  stagingArea(input = {}) {
    const state = this._state(); const normalizeEntries = entries => (entries || []).slice(0, 1000).map(item => ({ id: crypto.randomUUID(), sessionId: item.sessionId ? id(item.sessionId) : '', path: remotePath(item.path), kind: ['create', 'modify', 'delete', 'rename'].includes(item.kind) ? item.kind : 'modify', beforeHash: clean(item.beforeHash, 64), afterHash: clean(item.afterHash, 64), selected: item.selected !== false })); if (input.action === 'create') { const area = { id: crypto.randomUUID(), name: clean(input.name || 'Universal staging', 120), entries: normalizeEntries(input.entries), status: 'draft', createdAt: now() }; state.stagingAreas.push(area); this._save(state); return { success: true, area, summary: Object.fromEntries(['create', 'modify', 'delete', 'rename'].map(kind => [kind, area.entries.filter(item => item.kind === kind).length])), requiresApproval: true, executable: false, transaction: 'all-selected-or-rollback' }; } const area = state.stagingAreas.find(item => item.id === input.id); if (!area) throw new Error('Unknown staging area'); if (input.action === 'add') { area.entries.push(...normalizeEntries(input.entries)); this._save(state); } return { success: true, area, summary: Object.fromEntries(['create', 'modify', 'delete', 'rename'].map(kind => [kind, area.entries.filter(item => item.kind === kind).length])), requiresApproval: true, executable: false, transaction: 'all-selected-or-rollback' };
  }

  multiMonitorLayout(input = {}) { const state = this._state(); const displays = (input.displays || []).slice(0, 8).map(display => ({ id: clean(display.id, 120), width: Math.max(320, Number(display.width) || 1920), height: Math.max(240, Number(display.height) || 1080), scaleFactor: Math.max(0.5, Math.min(4, Number(display.scaleFactor) || 1)) })); if (!displays.length) throw new Error('At least one display is required'); const panels = (input.panels || []).slice(0, 20).map((panel, index) => ({ kind: ['terminal', 'files', 'hud', 'graph', 'inspector'].includes(panel.kind) ? panel.kind : 'terminal', displayId: displays.some(display => display.id === panel.displayId) ? panel.displayId : displays[index % displays.length].id, order: index })); const layout = { id: crypto.randomUUID(), name: clean(input.name || 'Operations layout', 120), displays, panels, serverId: input.sessionId ? id(input.sessionId) : '', secretsIncluded: false, createdAt: now() }; state.layouts.push(layout); this._save(state); return { success: true, layout, restore: 'window-placement-after-display-validation' };
  }

  bookmarks(input = {}) { const state = this._state(); if (input.action === 'list') return { bookmarks: state.bookmarks }; const kind = ['server', 'directory', 'file', 'line', 'container', 'command'].includes(input.kind) ? input.kind : 'file'; const bookmark = { id: crypto.randomUUID(), name: clean(input.name || 'Bookmark', 120), kind, sessionId: input.sessionId ? id(input.sessionId) : '', target: kind === 'directory' || kind === 'file' || kind === 'line' ? remotePath(input.target) : clean(input.target, 1000), line: kind === 'line' ? Math.max(1, Number(input.line) || 1) : 0, commandHash: kind === 'command' ? digest(input.target || '') : '', command: kind === 'command' ? redact(input.target) : '', tags: (input.tags || []).map(value => clean(value, 40)).slice(0, 20), createdAt: now(), credentialsIncluded: false }; state.bookmarks.push(bookmark); this._save(state); return { success: true, bookmark };
  }

  explainScreen(input = {}) { const kind = clean(input.kind, 40); const glossary = { process: ['A running program on the remote operating system.', ['inspect', 'signal', 'open-files']], file: ['A filesystem object. Verify owner, mode and provenance before editing.', ['preview', 'history', 'permissions']], permission: ['Unix mode or ACL determining effective access.', ['simulate', 'compare', 'stage-change']], port: ['A TCP/UDP endpoint that may be listening locally or publicly.', ['inspect-owner', 'create-tunnel', 'firewall-preview']], git: ['Repository state derived without changing the working tree.', ['diff', 'history', 'blame']], container: ['An isolated workload managed by Docker or Kubernetes.', ['logs', 'inspect', 'open-files']] }; const entry = glossary[kind]; return entry ? { understood: true, kind, explanation: entry[0], safeActions: entry[1], destructiveActionsShown: false, context: publicOnly(input.context || {}) } : { understood: false, kind, explanation: 'No deterministic explanation is available for this element.', safeActions: [], destructiveActionsShown: false };
  }

  workspaceHealth(input = {}) { const latency = Math.max(0, Number(input.latencyMs) || 0); const loss = Math.max(0, Math.min(100, Number(input.packetLossPercent) || 0)); const queue = Math.max(0, Number(input.transferQueue) || 0); const conflicts = Math.max(0, Number(input.conflicts) || 0); const tunnelExpiryMinutes = Math.max(0, Number(input.tunnelExpiryMinutes) || 0); const deductions = Math.min(100, Math.round(Math.min(35, latency / 10) + loss * 2 + Math.min(20, queue * 2) + Math.min(30, conflicts * 10) + (tunnelExpiryMinutes && tunnelExpiryMinutes < 10 ? 15 : 0))); const score = 100 - deductions; const issues = []; if (latency > 150) issues.push('high-latency'); if (loss > 2) issues.push('packet-loss'); if (queue > 5) issues.push('transfer-backlog'); if (conflicts) issues.push('offline-conflicts'); if (tunnelExpiryMinutes && tunnelExpiryMinutes < 10) issues.push('tunnel-expiring'); return { score, state: score >= 80 ? 'healthy' : score >= 50 ? 'degraded' : 'critical', issues, indicators: { latencyMs: latency, packetLossPercent: loss, transferQueue: queue, conflicts, tunnelExpiryMinutes }, compactByDefault: true };
  }

  disposableDataLens(input = {}) { const target = input.path ? remotePath(input.path) : clean(input.archive, 1000); if (!target) throw new Error('A file or archive is required'); const type = clean(input.type || path.posix.extname(target).slice(1) || 'unknown', 40); return { target, type, isolation: { process: 'disposable', network: false, credentials: false, filesystem: 'target-read-only', clipboard: false, childProcesses: false, timeoutSeconds: Math.max(5, Math.min(300, Number(input.timeoutSeconds) || 30)), memoryMb: Math.max(64, Math.min(2048, Number(input.memoryMb) || 256)) }, viewers: ['metadata', 'hex-preview', 'strings', 'safe-parser'], requiresApproval: true, executable: false, destroyedAfterClose: true };
  }

  crossServerCommandMatrix(input = {}) {
    const state = this._state(); const command = redact(input.command).replace(/[\r\n]/g, ' ').trim(); if (!command) throw new Error('A reviewed command is required'); const targets = [...new Set((input.sessionIds || []).map(value => id(value)))].slice(0, 200); if (!targets.length) throw new Error('At least one server is required'); const supplied = new Map((input.results || []).map(item => [id(item.sessionId), item])); const rows = targets.map(sessionId => { const result = supplied.get(sessionId); const output = result ? redact(result.output || result.stdout || '').slice(0, 256000) : ''; return { sessionId, status: result ? (Number(result.code) === 0 ? 'match-ready' : 'exception') : 'awaiting-read-only-run', code: result ? Number(result.code) || 0 : null, outputHash: result ? digest(output) : '', preview: output.split(/\r?\n/).slice(0, 8).join('\n') }; }); const groups = Object.values(rows.filter(row => row.outputHash).reduce((all, row) => { (all[row.outputHash] ||= { outputHash: row.outputHash, sessionIds: [], count: 0 }).sessionIds.push(row.sessionId); all[row.outputHash].count++; return all; }, {})); const matrix = { id: crypto.randomUUID(), commandHash: digest(command), commandPreview: command, targets, rows, groups, exceptions: rows.filter(row => row.status === 'exception').map(row => row.sessionId), createdAt: now(), outputLayout: 'grouped-differences', executable: false }; state.commandMatrices.push(matrix); this._save(state); return { success: true, matrix, requiresApproval: true };
  }

  transferControlCenter(input = {}) {
    const state = this._state(); const bandwidthMbps = Math.max(0, Math.min(100000, Number(input.bandwidthMbps) || 0)); const items = (input.items || []).slice(0, 2000).map((item, index) => { const bytes = Math.max(0, Number(item.bytes) || 0); const transferred = Math.max(0, Math.min(bytes, Number(item.transferred) || 0)); return { id: item.id ? id(item.id) : `transfer-${index + 1}`, source: publicOnly(item.source || {}), destination: publicOnly(item.destination || {}), bytes, transferred, remaining: bytes - transferred, progress: bytes ? Math.round(transferred / bytes * 100) : 0, priority: ['low', 'normal', 'high', 'critical'].includes(item.priority) ? item.priority : 'normal', state: transferred ? 'resumable' : 'queued', checkpoint: transferred ? digest(`${item.id || index}:${transferred}`).slice(0, 24) : '', retries: Math.max(0, Math.min(20, Number(item.retries) || 0)) }; }); const queue = { id: crypto.randomUUID(), name: clean(input.name || 'Transfer queue', 120), items, bandwidthMbps, concurrency: Math.max(1, Math.min(16, Number(input.concurrency) || 3)), retryPolicy: { maximum: 5, backoff: 'exponential-jitter' }, verification: 'sha256-after-transfer', totalBytes: items.reduce((sum, item) => sum + item.bytes, 0), remainingBytes: items.reduce((sum, item) => sum + item.remaining, 0), createdAt: now(), executable: false }; state.transferQueues.push(queue); this._save(state); return { success: true, queue, controls: ['pause', 'resume', 'reprioritize', 'retry', 'cancel'], requiresApproval: true };
  }

  directServerTransfer(input = {}) {
    const source = { sessionId: id(input.sourceSessionId), path: remotePath(input.sourcePath) }; const destination = { sessionId: id(input.destinationSessionId), path: remotePath(input.destinationPath) }; if (source.sessionId === destination.sessionId && source.path === destination.path) throw new Error('Source and destination must differ'); const directAvailable = input.directAvailable !== false; const route = directAvailable ? [source.sessionId, destination.sessionId] : [source.sessionId, 'encrypted-relay', destination.sessionId]; return { source, destination, bytes: Math.max(0, Number(input.bytes) || 0), route, mode: directAvailable ? 'server-to-server' : 'relay-stream', localPayloadBytes: 0, localDiskUsed: false, encryption: 'end-to-end', checks: ['source-read-access', 'destination-write-access', 'free-space', 'host-identity', 'sha256'], overwrite: false, resume: true, requiresApproval: true, executable: false };
  }

  atomicRemoteEditor(input = {}) {
    const state = this._state(); const target = remotePath(input.path); const before = String(input.beforeContent || '').slice(0, 2 * 1024 * 1024); const proposed = String(input.proposedContent || '').slice(0, 2 * 1024 * 1024); const beforeHash = digest(before); const proposedHash = digest(proposed); const expectedHash = clean(input.expectedHash || beforeHash, 64); if (!/^[a-f0-9]{64}$/i.test(expectedHash)) throw new Error('Expected hash must be SHA-256'); const changed = beforeHash !== proposedHash; const plan = { id: crypto.randomUUID(), sessionId: id(input.sessionId), path: target, beforeHash, proposedHash, expectedHash, changed, lines: { before: before.split(/\r?\n/).length, after: proposed.split(/\r?\n/).length }, lock: { mode: 'optimistic', compareHashImmediatelyBeforeWrite: true }, write: { temporaryPath: `${target}.kitsune-${crypto.randomUUID()}.tmp`, fsync: true, preserveModeOwner: true, atomicRename: true }, conflictStrategy: 'three-way-review', rollbackHash: beforeHash, contentPersisted: false, executable: false, createdAt: now() }; state.editorPlans.push(plan); this._save(state); return { success: true, plan, requiresApproval: changed };
  }

  sessionTimeMachine(input = {}) {
    const state = this._state(); const sessionId = id(input.sessionId); const events = (input.events || []).slice(0, 10000).map((event, index) => ({ sequence: index + 1, at: event.at || now(), kind: ['command', 'output', 'cwd', 'file-open', 'connection'].includes(event.kind) ? event.kind : 'output', command: event.command ? redact(event.command).slice(0, 4000) : '', output: event.output ? redact(event.output).slice(0, 16000) : '', path: event.path ? remotePath(event.path) : '', exitCode: event.exitCode === undefined ? null : Number(event.exitCode) })); const timeline = { id: crypto.randomUUID(), sessionId, name: clean(input.name || 'Terminal session', 120), events, chapters: events.filter(event => event.kind === 'cwd' || event.kind === 'connection').map(event => ({ sequence: event.sequence, label: event.kind === 'cwd' ? event.path : 'Connection changed' })), searchableTextHash: digest(events.map(event => `${event.command}\n${event.output}\n${event.path}`).join('\n')), secretsIncluded: false, replay: { restoresContextOnly: true, commandsExecuted: false }, createdAt: now() }; state.sessionTimelines.push(timeline); this._save(state); return { success: true, timeline };
  }

  filesystemSnapshotDiff(input = {}) {
    const normalize = entries => (entries || []).slice(0, 100000).map(item => ({ path: remotePath(item.path), sha256: /^[a-f0-9]{64}$/i.test(item.sha256 || '') ? item.sha256.toLowerCase() : digest(item.content || ''), bytes: Math.max(0, Number(item.bytes) || 0), mode: clean(item.mode || '', 8), owner: clean(item.owner || '', 120) })); const before = normalize(input.before); const after = normalize(input.after); const oldMap = new Map(before.map(item => [item.path, item])); const newMap = new Map(after.map(item => [item.path, item])); const paths = [...new Set([...oldMap.keys(), ...newMap.keys()])].sort(); const changes = paths.map(file => { const oldItem = oldMap.get(file); const newItem = newMap.get(file); const kind = !oldItem ? 'added' : !newItem ? 'removed' : oldItem.sha256 !== newItem.sha256 ? 'content' : oldItem.mode !== newItem.mode || oldItem.owner !== newItem.owner ? 'metadata' : 'unchanged'; return { path: file, kind, before: oldItem ? publicOnly(oldItem) : null, after: newItem ? publicOnly(newItem) : null }; }).filter(item => item.kind !== 'unchanged'); return { root: remotePath(input.root || '/'), changes, summary: Object.fromEntries(['added', 'removed', 'content', 'metadata'].map(kind => [kind, changes.filter(item => item.kind === kind).length])), manifests: { before: digest(JSON.stringify(before)), after: digest(JSON.stringify(after)) }, serverMutations: false, executable: false };
  }

  smartSyncProfile(input = {}) {
    const state = this._state(); const direction = ['download', 'upload', 'bidirectional'].includes(input.direction) ? input.direction : 'bidirectional'; const profile = { id: crypto.randomUUID(), name: clean(input.name || 'Smart sync', 120), sessionId: id(input.sessionId), remoteRoot: remotePath(input.remoteRoot || '/'), localRoot: path.resolve(clean(input.localRoot, 1000)), direction, includes: (input.includes || []).map(value => clean(value, 240)).slice(0, 100), excludes: (input.excludes || []).map(value => clean(value, 240)).slice(0, 100), conflictStrategy: ['ask', 'keep-both', 'three-way'].includes(input.conflictStrategy) ? input.conflictStrategy : 'three-way', deletePropagation: false, schedule: clean(input.schedule || 'manual', 80), safeguards: ['dry-run', 'protected-path-check', 'free-space-check', 'rollback-manifest'], credentialsIncluded: false, status: 'draft', executable: false, createdAt: now() }; state.syncProfiles.push(profile); this._save(state); return { success: true, profile, requiresApproval: direction !== 'download' };
  }

  archiveExplorer(input = {}) {
    const archive = remotePath(input.path); const type = /\.tar\.gz$/i.test(archive) ? 'tar.gz' : path.posix.extname(archive).slice(1).toLowerCase(); if (!['zip', 'tar', 'gz', 'tar.gz', '7z'].includes(type)) throw new Error('Unsupported archive format'); const entries = (input.entries || []).slice(0, 100000).map(item => { const name = clean(typeof item === 'string' ? item : item.name, 1000).replace(/\\/g, '/'); const normalized = path.posix.normalize(`/${name}`).slice(1); const unsafe = name.startsWith('/') || name.split('/').includes('..') || name.includes('\0'); return { name: unsafe ? '<blocked-entry>' : normalized, bytes: Math.max(0, Number(item.bytes) || 0), directory: Boolean(item.directory), safe: !unsafe }; }); return { archive, type, entries, unsafeEntries: entries.filter(item => !item.safe).length, navigation: 'folder-tree', previewLimitBytes: 4 * 1024 * 1024, extraction: { overwrite: false, preservePaths: true, validateEveryEntry: true }, remoteArchiveDownloaded: false, requiresApprovalToExtract: true, executable: false };
  }

  remoteStorageFabric(input = {}) {
    const supported = new Set(['sftp', 'scp', 'ftp', 'ftps', 'smb', 'webdav', 's3', 'azure-blob', 'docker', 'kubernetes', 'wsl', 'local']); const endpoints = (input.endpoints || []).slice(0, 100).map((item, index) => { const protocol = clean(item.protocol, 40).toLowerCase(); if (!supported.has(protocol)) throw new Error(`Unsupported storage protocol: ${protocol}`); return { id: item.id ? id(item.id) : `storage-${index + 1}`, name: clean(item.name || protocol, 120), protocol, root: protocol === 'local' ? path.resolve(clean(item.root || '.', 1000)) : remotePath(item.root || '/'), capabilities: { browse: true, read: true, write: !['docker', 'kubernetes'].includes(protocol) || Boolean(item.writeEnabled), atomicRename: ['sftp', 'scp', 'smb', 'webdav', 'local'].includes(protocol), serverSideCopy: ['s3', 'azure-blob', 'smb'].includes(protocol) }, credentialsIncluded: false }; }); return { endpoints, unifiedActions: ['browse', 'preview', 'copy-plan', 'compare', 'bookmark'], protocolSpecificDetailsCollapsed: true, dragDropRequiresPlan: true, executable: false };
  }

  liveVirtualFolders(input = {}) {
    const state = this._state(); const allowedFields = new Set(['name', 'path', 'extension', 'bytes', 'modifiedMinutes', 'kind', 'server']); const conditions = (input.conditions || []).slice(0, 20).map(condition => { const field = clean(condition.field, 40); if (!allowedFields.has(field)) throw new Error(`Unsupported virtual-folder field: ${field}`); return { field, operator: ['contains', 'equals', 'gt', 'lt'].includes(condition.operator) ? condition.operator : 'contains', value: clean(condition.value, 240) }; }); const records = (input.records || []).slice(0, 100000).map(publicOnly); const matches = records.filter(record => conditions.every(condition => { const actual = record[condition.field]; if (condition.operator === 'contains') return String(actual || '').toLowerCase().includes(condition.value.toLowerCase()); if (condition.operator === 'equals') return String(actual) === condition.value; if (condition.operator === 'gt') return Number(actual) > Number(condition.value); return Number(actual) < Number(condition.value); })).slice(0, 1000); const folder = { id: crypto.randomUUID(), name: clean(input.name || 'Live collection', 120), conditions, sourceSessionIds: (input.sessionIds || []).map(value => id(value)).slice(0, 100), refresh: 'incremental-events', resultCount: matches.length, createdAt: now() }; state.virtualFolders.push(folder); this._save(state); return { success: true, folder, preview: matches, contentCopied: false };
  }

  diskSpaceVisualizer(input = {}) {
    const entries = (input.entries || []).slice(0, 200000).map(item => ({ path: remotePath(item.path), bytes: Math.max(0, Number(item.bytes) || 0), kind: clean(item.kind || 'file', 20) })); const root = remotePath(input.root || '/'); const groups = new Map(); for (const item of entries) { const relative = path.posix.relative(root, item.path); if (relative.startsWith('..')) continue; const segment = relative.split('/')[0] || path.posix.basename(root) || '/'; const current = groups.get(segment) || { name: segment, bytes: 0, files: 0 }; current.bytes += item.bytes; current.files++; groups.set(segment, current); } const nodes = [...groups.values()].sort((a, b) => b.bytes - a.bytes); return { root, totalBytes: nodes.reduce((sum, item) => sum + item.bytes, 0), visualization: 'zoomable-treemap', nodes, cleanupCandidates: entries.filter(item => item.kind === 'cache' || item.kind === 'temporary').sort((a, b) => b.bytes - a.bytes).slice(0, 50), cleanupExecutable: false, serverMutations: false };
  }

  processPortContainerNavigator(input = {}) {
    const processes = (input.processes || []).slice(0, 10000).map(item => ({ pid: Math.max(1, Number(item.pid) || 1), name: clean(item.name, 120), user: clean(item.user, 120), containerId: clean(item.containerId, 120), configPaths: (item.configPaths || []).map(remotePath).slice(0, 20) })); const processMap = new Map(processes.map(item => [item.pid, item])); const ports = (input.ports || []).slice(0, 10000).map(item => ({ port: Math.max(1, Math.min(65535, Number(item.port) || 1)), protocol: ['tcp', 'udp'].includes(item.protocol) ? item.protocol : 'tcp', address: clean(item.address || '127.0.0.1', 64), pid: Math.max(1, Number(item.pid) || 1), public: !['127.0.0.1', '::1', 'localhost'].includes(item.address) })); const edges = ports.map(item => ({ from: `port:${item.protocol}:${item.port}`, to: `process:${item.pid}`, resolved: processMap.has(item.pid) })).concat(processes.filter(item => item.containerId).map(item => ({ from: `process:${item.pid}`, to: `container:${item.containerId}`, resolved: true }))); return { nodes: { processes, ports, containers: [...new Set(processes.map(item => item.containerId).filter(Boolean))] }, edges, safeActions: ['open-logs', 'open-config', 'copy-address', 'tunnel-preview'], destructiveActionsCollapsed: true, serverMutations: false };
  }

  secureClipboardBridge(input = {}) {
    const content = String(input.content || '').slice(0, 1024 * 1024); const sanitized = redact(content); const sensitive = sanitized !== content || /-----BEGIN|\b(?:AKIA[0-9A-Z]{16}|gh[pousr]_[A-Za-z0-9_]{20,})\b/.test(content); const seconds = Math.max(5, Math.min(300, Number(input.expiresSeconds) || 30)); return { bytes: Buffer.byteLength(content), contentHash: digest(content), preview: sensitive ? '<sensitive-content-blocked>' : sanitized.slice(0, 240), sensitive, allowed: !sensitive && Buffer.byteLength(content) <= 1024 * 1024, expiresAt: new Date(Date.now() + seconds * 1000).toISOString(), clearAfterPaste: true, historyPersisted: false, destinations: (input.destinations || []).map(value => clean(value, 40)).filter(value => ['local', 'ssh', 'rdp'].includes(value)), executable: false };
  }

  shellResurrection(input = {}) {
    const sessionId = id(input.sessionId); const tabs = (input.tabs || []).slice(0, 100).map(tab => ({ name: clean(tab.name || 'Terminal', 120), cwd: remotePath(tab.cwd || '/'), multiplexer: ['tmux', 'screen', 'none'].includes(tab.multiplexer) ? tab.multiplexer : 'none', remoteSession: clean(tab.remoteSession, 120), commandHash: tab.command ? digest(tab.command) : '', commandRestored: false })); return { sessionId, tabs, strategy: tabs.some(tab => tab.multiplexer === 'tmux') ? 'tmux-reattach' : tabs.some(tab => tab.multiplexer === 'screen') ? 'screen-reattach' : 'context-only', order: ['verify-host-identity', 'restore-connection', 'reattach-multiplexer', 'restore-cwd', 'restore-layout'], commandsReexecuted: false, secretsIncluded: false, requiresApproval: true, executable: false };
  }

  terminalFileManagerFusion(input = {}) {
    const cwd = remotePath(input.cwd || '/'); const shell = ['posix', 'powershell', 'cmd'].includes(input.shell) ? input.shell : 'posix'; const selected = (input.paths || []).slice(0, 100).map(remotePath); const quote = value => shell === 'powershell' ? `'${value.replace(/'/g, "''")}'` : shell === 'cmd' ? `"${value.replace(/"/g, '""')}"` : `'${value.replace(/'/g, `'"'"'`)}'`; const output = redact(input.output || '').slice(0, 256000); const detectedPaths = [...new Set([...output.matchAll(/(?:^|\s)(\/[A-Za-z0-9_./@+-]+)/gm)].map(match => { try { return remotePath(match[1]); } catch { return ''; } }).filter(Boolean))].slice(0, 100); return { cwd, shell, selectedPaths: selected, quotedSelection: selected.map(quote).join(' '), detectedPaths, interactions: ['terminal-cwd-follows-file-pane', 'file-pane-follows-cd', 'drag-path-to-command', 'open-output-path', 'send-output-to-data-canvas'], unsafeAutomaticExecution: false, executable: false };
  }

  executionContextBeacon(input = {}) {
    const environment = ['production', 'staging', 'development', 'unknown'].includes(input.environment) ? input.environment : 'unknown'; const actual = { sessionId: input.sessionId ? id(input.sessionId) : '', host: clean(input.host || 'local', 253), user: clean(input.user || 'unknown', 120), elevated: Boolean(input.elevated), runtime: ['host', 'docker', 'kubernetes', 'wsl'].includes(input.runtime) ? input.runtime : 'host', target: clean(input.target, 240), namespace: clean(input.namespace, 120), cwd: input.cwd ? remotePath(input.cwd) : '/', environment }; const expected = publicOnly(input.expected || {}); const mismatches = Object.entries(expected).filter(([key, value]) => value !== '' && value !== undefined && String(actual[key]) !== String(value)).map(([key, value]) => ({ field: key, expected: clean(value, 240), actual: clean(actual[key], 240) })); const danger = environment === 'production' || actual.elevated || mismatches.length > 0; return { context: actual, badges: [environment, actual.elevated ? 'elevated' : 'standard-user', actual.runtime, actual.namespace || actual.target || 'host'].filter(Boolean), theme: environment === 'production' ? 'critical-red' : environment === 'staging' ? 'warning-amber' : 'verified-blue', mismatches, blocked: mismatches.length > 0, confirmationLevel: danger ? 'context-and-command' : 'standard', credentialsIncluded: false, compact: true };
  }

  connectionWaterfallTuner(input = {}) {
    const allowed = ['dns', 'tcp', 'proxy', 'vpn', 'tls', 'ssh', 'authentication', 'channel', 'sftp']; const phases = (input.phases || []).slice(0, allowed.length).map((phase, index) => ({ id: allowed.includes(phase.id) ? phase.id : allowed[index], durationMs: Math.max(0, Math.min(120000, Number(phase.durationMs) || 0)), status: ['ok', 'slow', 'failed', 'skipped'].includes(phase.status) ? phase.status : 'ok' })); const totalMs = phases.reduce((sum, phase) => sum + phase.durationMs, 0); const bottleneck = phases.filter(phase => phase.status !== 'skipped').sort((a, b) => b.durationMs - a.durationMs)[0] || null; const recommendations = []; if (phases.find(phase => phase.id === 'dns')?.durationMs > 250) recommendations.push('cache-verified-dns'); if (phases.find(phase => phase.id === 'ssh')?.durationMs > 500) recommendations.push('reuse-ssh-channel'); if (phases.find(phase => phase.id === 'sftp')?.durationMs > 500) recommendations.push('increase-bounded-sftp-window'); if (Number(input.latencyMs) > 120) recommendations.push('enable-compression-for-text-only'); return { phases, totalMs, bottleneck, recommendations, visualization: 'waterfall', settingsApplied: false, requiresBenchmarkApproval: true, credentialsIncluded: false };
  }

  dataBoundaryGuard(input = {}) {
    const content = String(input.content || '').slice(0, 4 * 1024 * 1024); const sourceZone = ['public', 'internal', 'confidential', 'restricted'].includes(input.sourceZone) ? input.sourceZone : 'internal'; const destinationZone = ['public', 'internal', 'confidential', 'restricted'].includes(input.destinationZone) ? input.destinationZone : 'internal'; const findings = []; const checks = [{ kind: 'private-key', re: /-----BEGIN [^-]+PRIVATE KEY-----/g, severity: 'critical' }, { kind: 'credential', re: /\b(?:password|passwd|secret|token|api[_-]?key)\s*[=:]\s*\S+/gi, severity: 'critical' }, { kind: 'email-address', re: /\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi, severity: 'medium' }, { kind: 'database-dump', re: /(?:CREATE TABLE|INSERT INTO|pg_dump|mysqldump)/gi, severity: 'high' }]; for (const check of checks) { const count = [...content.matchAll(check.re)].length; if (count) findings.push({ kind: check.kind, severity: check.severity, count }); } const rank = { public: 0, internal: 1, confidential: 2, restricted: 3 }; const crossesOutward = rank[destinationZone] < rank[sourceZone]; const blocked = findings.some(item => item.severity === 'critical') || (crossesOutward && findings.length > 0); return { sourceZone, destinationZone, crossesTrustBoundary: sourceZone !== destinationZone, outward: crossesOutward, findings, contentHash: digest(content), contentPersisted: false, allowed: !blocked, blocked, requiresDataOwnerApproval: crossesOutward, executable: false };
  }

  liveOutputPrivacyShield(input = {}) {
    const output = String(input.output || '').slice(0, 4 * 1024 * 1024); let sanitized = redact(output); const findings = []; const replace = (kind, regex, replacement) => { let count = 0; sanitized = sanitized.replace(regex, match => { count++; return replacement || `<${kind}-redacted>`; }); if (count) findings.push({ kind, count }); }; replace('email', /\b[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}\b/gi); replace('ipv4', /\b(?:\d{1,3}\.){3}\d{1,3}\b/g); replace('bearer', /\bBearer\s+[A-Za-z0-9._~+/-]+=*/gi); if (sanitized !== output && !findings.some(item => item.kind === 'secret')) findings.unshift({ kind: 'secret', count: 1 }); return { sanitized, originalHash: digest(output), originalPersisted: false, findings, protectedActions: ['copy', 'share', 'record', 'export'], shieldActive: true, revealRequiresLocalConfirmation: sanitized !== output };
  }

  detachedJobOrchestrator(input = {}) {
    const state = this._state(); const command = redact(input.command).replace(/[\r\n]/g, ' ').trim(); if (!command) throw new Error('A reviewed command is required'); const job = { id: crypto.randomUUID(), sessionId: id(input.sessionId), name: clean(input.name || 'Detached job', 120), commandHash: digest(command), commandPreview: command.slice(0, 500), runner: ['systemd-run', 'tmux', 'screen', 'agent'].includes(input.runner) ? input.runner : 'systemd-run', budgets: { timeoutSeconds: Math.max(10, Math.min(604800, Number(input.timeoutSeconds) || 3600)), outputBytes: Math.max(1024, Math.min(1024 ** 3, Number(input.outputBytes) || 16 * 1024 * 1024)), cpuPercent: Math.max(1, Math.min(100, Number(input.cpuPercent) || 50)), memoryMb: Math.max(32, Math.min(131072, Number(input.memoryMb) || 1024)) }, controls: ['attach', 'tail', 'cancel-with-grace', 'collect-result'], status: 'planned', survivesClientExit: true, credentialsIncluded: false, executable: false, createdAt: now() }; state.detachedJobs.push(job); this._save(state); return { success: true, job, requiresApproval: true };
  }

  conflictResolutionCockpit(input = {}) {
    const state = this._state(); const target = remotePath(input.path); const base = String(input.base || '').slice(0, 2 * 1024 * 1024); const local = String(input.local || '').slice(0, 2 * 1024 * 1024); const remote = String(input.remote || '').slice(0, 2 * 1024 * 1024); let result = ''; let status = 'conflict'; if (local === remote) { result = local; status = 'identical'; } else if (local === base) { result = remote; status = 'remote-only'; } else if (remote === base) { result = local; status = 'local-only'; } else result = `<<<<<<< LOCAL\n${local}\n||||||| BASE\n${base}\n=======\n${remote}\n>>>>>>> REMOTE`; const record = { id: crypto.randomUUID(), sessionId: input.sessionId ? id(input.sessionId) : '', path: target, hashes: { base: digest(base), local: digest(local), remote: digest(remote), result: digest(result) }, status, regions: status === 'conflict' ? [{ kind: 'conflict', startLine: 1, choices: ['local', 'base', 'remote', 'manual'] }] : [], contentPersisted: false, createdAt: now() }; state.conflictCockpits.push(record); this._save(state); return { success: true, cockpit: record, result: redact(result), layout: 'three-way-plus-result', executable: false, requiresApproval: status !== 'identical' };
  }

  executableTrustInspector(input = {}) {
    const resolvedPath = remotePath(input.resolvedPath); const kind = ['binary', 'script', 'alias', 'function', 'builtin'].includes(input.kind) ? input.kind : 'binary'; const mode = clean(input.mode || '755', 8); const worldWritable = /^[0-7]{3,4}$/.test(mode) && (Number(mode.at(-1)) & 2) === 2; const sha256 = clean(input.sha256, 64); if (sha256 && !/^[a-f0-9]{64}$/i.test(sha256)) throw new Error('Executable hash must be SHA-256'); const risks = []; if (worldWritable) risks.push('world-writable'); if (input.owner && !['root', 'SYSTEM', input.user].includes(input.owner)) risks.push('unexpected-owner'); if (input.signature === 'invalid') risks.push('invalid-signature'); if (!sha256 && ['binary', 'script'].includes(kind)) risks.push('hash-unavailable'); return { name: clean(input.name || path.posix.basename(resolvedPath), 120), kind, resolvedPath, owner: clean(input.owner || 'unknown', 120), mode, package: clean(input.package, 240), sha256, signature: ['valid', 'invalid', 'unsigned', 'unknown'].includes(input.signature) ? input.signature : 'unknown', aliasTarget: clean(input.aliasTarget, 1000), risks, trusted: risks.length === 0 && input.signature !== 'invalid', commandExecuted: false, inspection: 'read-only' };
  }

  resourceBudgetedCommand(input = {}) {
    const command = redact(input.command).replace(/[\r\n]/g, ' ').trim(); if (!command) throw new Error('A reviewed command is required'); return { sessionId: id(input.sessionId), commandHash: digest(command), commandPreview: command.slice(0, 500), budgets: { timeoutSeconds: Math.max(1, Math.min(86400, Number(input.timeoutSeconds) || 300)), cpuPercent: Math.max(1, Math.min(100, Number(input.cpuPercent) || 50)), memoryMb: Math.max(16, Math.min(131072, Number(input.memoryMb) || 512)), outputBytes: Math.max(1024, Math.min(1024 ** 3, Number(input.outputBytes) || 8 * 1024 * 1024)), networkBytes: Math.max(0, Math.min(1024 ** 4, Number(input.networkBytes) || 0)) }, enforcement: ['wall-clock-timeout', 'process-group', 'bounded-output', 'graceful-then-force'], partialDiagnosticsRetained: true, executable: false, requiresApproval: true };
  }

  remoteQuarantineLab(input = {}) {
    const state = this._state(); const target = remotePath(input.path); const sample = Buffer.from(String(input.sample || '').slice(0, 4 * 1024 * 1024)); const strings = sample.toString('utf8'); const findings = []; if (/MZ/.test(strings.slice(0, 2))) findings.push({ kind: 'windows-executable', severity: 'medium' }); if (/-----BEGIN [^-]+PRIVATE KEY-----/.test(strings)) findings.push({ kind: 'private-key', severity: 'critical' }); if (/powershell.+(?:downloadstring|invoke-webrequest)|curl.+\|.+sh/i.test(strings)) findings.push({ kind: 'download-execute', severity: 'critical' }); const entropy = sample.length ? [...new Set(sample)].length / 256 : 0; if (entropy > 0.8) findings.push({ kind: 'high-entropy', severity: 'medium' }); const record = { id: crypto.randomUUID(), sessionId: input.sessionId ? id(input.sessionId) : '', path: target, sha256: digest(sample), bytesSampled: sample.length, findings, isolation: { network: false, credentials: false, execution: false, filesystem: 'quarantine-read-only', clipboard: false }, analyzers: ['magic', 'mime', 'strings', 'entropy', 'archive-depth', 'yara-compatible-rules'], samplePersisted: false, createdAt: now() }; state.quarantines.push(record); this._save(state); return { success: true, quarantine: record, executable: false };
  }

  branchableTerminal(input = {}) {
    const state = this._state(); const backend = ['tmux', 'container', 'disposable-workspace'].includes(input.backend) ? input.backend : 'tmux'; const branch = { id: crypto.randomUUID(), sessionId: id(input.sessionId), name: clean(input.name || 'Terminal branch', 120), backend, source: { cwd: remotePath(input.cwd || '/'), environmentHash: digest(JSON.stringify(publicOnly(input.environment || {}))), openFiles: (input.openFiles || []).map(remotePath).slice(0, 100) }, isolation: backend === 'tmux' ? 'process-session' : 'filesystem-and-process', writable: backend !== 'tmux' && Boolean(input.writable), commandsCopied: false, commandsExecuted: false, secretsIncluded: false, expiryMinutes: Math.max(5, Math.min(1440, Number(input.expiryMinutes) || 120)), status: 'planned', createdAt: now() }; state.terminalBranches.push(branch); this._save(state); return { success: true, branch, requiresApproval: branch.writable, executable: false };
  }

  adaptiveSshChannelPool(input = {}) {
    const connections = (input.connections || []).slice(0, 500).map(item => ({ sessionId: id(item.sessionId), host: clean(item.host, 253), user: clean(item.user, 120), port: Math.max(1, Math.min(65535, Number(item.port) || 22)), activeChannels: Math.max(0, Math.min(100, Number(item.activeChannels) || 0)), idleSeconds: Math.max(0, Number(item.idleSeconds) || 0) })); const groups = Object.values(connections.reduce((all, item) => { const key = `${item.user}@${item.host}:${item.port}`; all[key] ||= { key, sessionIds: [], activeChannels: 0, maximumChannels: Math.max(2, Math.min(32, Number(input.maximumChannels) || 8)), keepAliveSeconds: Math.max(10, Math.min(300, Number(input.keepAliveSeconds) || 30)), expireIdleSeconds: Math.max(30, Math.min(3600, Number(input.expireIdleSeconds) || 300)) }; all[key].sessionIds.push(item.sessionId); all[key].activeChannels += item.activeChannels; return all; }, {})); return { pools: groups, strategy: 'verified-control-channel-reuse', hostIdentitySharedAcrossChannels: false, reauthenticationOnIdentityChange: true, privateKeysPersisted: false, settingsApplied: false, executable: false };
  }

  terminalFilesReviewInbox(input = {}) {
    const state = this._state(); if (input.action === 'list') return { items: state.reviewInbox.map(publicOnly), pending: state.reviewInbox.filter(item => item.status === 'pending').length, layout: 'risk-grouped-cards' }; if (input.action === 'decide') { const item = state.reviewInbox.find(value => value.id === input.id); if (!item) throw new Error('Unknown review item'); const decision = ['approved', 'rejected'].includes(input.decision) ? input.decision : 'rejected'; const approver = clean(input.approver, 120); if (!approver || approver === item.requestedBy) throw new Error('A distinct approver is required'); item.status = decision; item.decision = { approver, comment: clean(input.comment, 1000), at: now() }; this._save(state); return { success: true, item: publicOnly(item), executable: false }; } const risk = ['read', 'write', 'destructive', 'restricted'].includes(input.risk) ? input.risk : 'write'; const item = { id: crypto.randomUUID(), kind: clean(input.kind || 'file-change', 80), title: clean(input.title || 'Terminal & Files change', 160), sessionId: input.sessionId ? id(input.sessionId) : '', target: clean(input.target, 1000), risk, requestedBy: clean(input.requestedBy || 'Local owner', 120), planHash: digest(JSON.stringify(publicOnly(input.plan || {}))), preview: redact(input.preview || '').slice(0, 1000), status: 'pending', credentialsIncluded: false, createdAt: now() }; state.reviewInbox.push(item); this._save(state); return { success: true, item, layout: 'risk-card', executable: false };
  }

  async approvedExecutionEngine(input = {}) {
    const state = this._state(); const action = input.action || 'create';
    if (action === 'create') {
      const sessionId = id(input.sessionId); const requestedBy = clean(input.requestedBy || 'Local owner', 120); const payload = (input.actions || []).slice(0, 20).map((item, index) => { if (item.kind === 'safe-template') { const template = clean(item.template, 40); if (!['disk', 'ports', 'tail', 'journal', 'dockerLogs'].includes(template)) throw new Error('Execution plans accept only bounded command templates'); return { kind: 'safe-template', template, parameters: publicOnly(item.parameters || {}), timeoutMs: Math.max(1000, Math.min(120000, Number(item.timeoutMs) || 30000)) }; } if (item.kind === 'file-write') { const target = remotePath(item.path); const content = String(item.content || ''); if (Buffer.byteLength(content) > 2 * 1024 * 1024) throw new Error('Approved file writes are limited to 2 MB'); return { kind: 'file-write', path: target, content, expectedHash: clean(item.expectedHash, 64) }; } throw new Error(`Unsupported approved action ${index + 1}`); }); if (!payload.length) throw new Error('At least one approved action is required'); const planId = crypto.randomUUID(); const publicActions = payload.map(item => item.kind === 'file-write' ? { kind: item.kind, path: item.path, bytes: Buffer.byteLength(item.content), contentHash: digest(item.content), expectedHash: item.expectedHash } : item); const signed = { id: planId, sessionId, actions: publicActions, idempotencyKey: crypto.randomUUID(), requestedBy, createdAt: now() }; const signature = crypto.createHmac('sha256', this._key('execution-signing')).update(JSON.stringify(signed)).digest('hex'); const plan = { ...signed, signature, status: 'pending-approval', approver: '', appliedAt: '', result: null, executable: false }; this.secretStore.set(`terminal-file-vision:execution:${planId}`, JSON.stringify(payload)); state.executionPlans.push(plan); this._save(state); return { success: true, plan: publicOnly(plan), next: 'distinct-approval' };
    }
    const plan = state.executionPlans.find(item => item.id === input.id); if (!plan) throw new Error('Unknown execution plan'); const signed = { id: plan.id, sessionId: plan.sessionId, actions: plan.actions, idempotencyKey: plan.idempotencyKey, requestedBy: plan.requestedBy, createdAt: plan.createdAt }; const validSignature = crypto.createHmac('sha256', this._key('execution-signing')).update(JSON.stringify(signed)).digest('hex') === plan.signature; if (!validSignature) throw new Error('Execution plan integrity failed');
    if (action === 'approve') { const approver = clean(input.approver, 120); if (!approver || approver === plan.requestedBy) throw new Error('A distinct approver is required'); if (plan.status !== 'pending-approval') throw new Error('Plan is not awaiting approval'); plan.approver = approver; plan.status = 'approved'; plan.approvedAt = now(); this._save(state); return { success: true, plan: publicOnly(plan), executable: false }; }
    if (action !== 'apply') throw new Error('Unsupported execution action'); if (plan.status === 'completed') return { success: true, idempotent: true, plan: publicOnly(plan), result: plan.result }; if (plan.status !== 'approved' || input.approved !== true) throw new Error('Approved execution requires an approved plan and explicit consent'); if (clean(input.idempotencyKey, 80) !== plan.idempotencyKey) throw new Error('Idempotency key does not match'); const session = this.remote?.list().find(item => item.id === plan.sessionId); if (!session) throw new Error('Execution server profile is unavailable'); const raw = this.secretStore.get(`terminal-file-vision:execution:${plan.id}`); if (!raw) throw new Error('Execution payload has expired'); const payload = JSON.parse(raw); const applied = []; const rollback = [];
    try { for (const item of payload) { if (item.kind === 'safe-template') { const generated = this.advanced.safeCommand(item.template, item.parameters); const result = await this.ops.exec(session, generated.command, item.timeoutMs); applied.push({ kind: item.kind, template: item.template, success: result.success, code: result.code, stdout: redact(result.stdout || '').slice(0, 256000), stderr: redact(result.stderr || '').slice(0, 64000) }); if (!result.success) throw new Error(result.stderr || `Template ${item.template} failed`); } else { let before = null; try { before = await this.remote.readRemote(session, item.path, 2 * 1024 * 1024); } catch {} if (item.expectedHash && (!before || digest(before.content) !== item.expectedHash)) throw new Error(`Concurrent file change detected for ${item.path}`); rollback.push({ path: item.path, existed: Boolean(before), content: before?.content || '' }); const written = await this.remote.writeRemote(session, item.path, item.content, 2 * 1024 * 1024); const verified = await this.remote.readRemote(session, item.path, 2 * 1024 * 1024); if (digest(verified.content) !== digest(item.content)) throw new Error(`Verification failed for ${item.path}`); applied.push({ kind: item.kind, path: item.path, success: written.success !== false, sha256: digest(item.content) }); } } } catch (error) { const rolledBack = []; for (const item of rollback.reverse()) { try { if (item.existed) await this.remote.writeRemote(session, item.path, item.content, 2 * 1024 * 1024); else await this.remote.mutate(session, 'delete', item.path); rolledBack.push({ path: item.path, success: true }); } catch (rollbackError) { rolledBack.push({ path: item.path, success: false, error: rollbackError.message }); } } plan.status = 'failed-rolled-back'; plan.result = { success: false, error: clean(error.message, 2000), applied, rolledBack }; this._save(state); return { success: false, plan: publicOnly(plan), result: plan.result }; }
    plan.status = 'completed'; plan.appliedAt = now(); plan.executable = true; plan.result = { success: true, applied, verified: true, rollbackRequired: false }; this.secretStore.remove(`terminal-file-vision:execution:${plan.id}`); this._save(state); return { success: true, plan: publicOnly(plan), result: plan.result };
  }

  visualResultRenderers(input = {}) {
    const kind = ['waterfall', 'treemap', 'matrix', 'graph', 'conflict', 'timeline', 'transfer'].includes(input.kind) ? input.kind : 'timeline'; const data = publicOnly(input.data || {}); const layouts = { waterfall: { axis: 'duration-ms', mark: 'phase-bar' }, treemap: { hierarchy: 'path', mark: 'area' }, matrix: { rows: 'targets', columns: 'signals' }, graph: { nodes: 'entities', edges: 'relations' }, conflict: { panes: ['base', 'local', 'remote', 'result'] }, timeline: { axis: 'time', mark: 'event' }, transfer: { axis: 'bytes', mark: 'progress-lane' } }; return { kind, renderer: layouts[kind], data, rawAvailable: true, maximumItems: Math.max(10, Math.min(100000, Number(input.maximumItems) || 5000)), virtualized: true, actions: ['inspect', 'filter', 'copy-sanitized', 'export-view'], mutationsAllowed: false };
  }

  unifiedRemoteSessionCanvas(input = {}) {
    const state = this._state(); const allowed = new Set(['ssh', 'sftp', 'powershell', 'wsl', 'docker-exec', 'kubernetes-exec', 'rdp', 'vnc', 'serial']); const panes = (input.panes || []).slice(0, 12).map((pane, index) => { const protocol = clean(pane.protocol, 40); if (!allowed.has(protocol)) throw new Error(`Unsupported canvas protocol: ${protocol}`); return { id: pane.id ? id(pane.id) : `pane-${index + 1}`, protocol, sessionId: pane.sessionId ? id(pane.sessionId) : '', title: clean(pane.title || protocol, 120), role: ['terminal', 'files', 'desktop', 'console'].includes(pane.role) ? pane.role : ['rdp', 'vnc'].includes(protocol) ? 'desktop' : protocol === 'sftp' ? 'files' : 'terminal', contextSync: pane.contextSync !== false, credentialsIncluded: false }; }); if (!panes.length) throw new Error('Session Canvas requires at least one pane'); const canvas = { id: crypto.randomUUID(), name: clean(input.name || 'Remote Session Canvas', 120), layout: ['tabs', 'split', 'grid', 'focus'].includes(input.layout) ? input.layout : 'split', panes, sharedContext: { sessionId: input.sessionId ? id(input.sessionId) : '', cwd: input.cwd ? remotePath(input.cwd) : '/', clipboardPolicy: ['disabled', 'text-only', 'guarded-files'].includes(input.clipboardPolicy) ? input.clipboardPolicy : 'text-only' }, handoff: 'encrypted-context-no-credentials', createdAt: now() }; state.sessionCanvases.push(canvas); this._save(state); return { success: true, canvas };
  }

  remoteDesktopQualityEngine(input = {}) {
    const latency = Math.max(0, Number(input.latencyMs) || 0); const bandwidth = Math.max(0.1, Number(input.bandwidthMbps) || 10); const loss = Math.max(0, Math.min(100, Number(input.packetLossPercent) || 0)); const requested = ['quality', 'balanced', 'low-latency', 'auto'].includes(input.mode) ? input.mode : 'auto'; const mode = requested !== 'auto' ? requested : latency > 180 || loss > 3 || bandwidth < 5 ? 'low-latency' : latency < 50 && bandwidth > 30 ? 'quality' : 'balanced'; const profiles = { quality: { fps: 60, colorDepth: 32, imageQuality: 90, audio: true, wallpaper: true }, balanced: { fps: 30, colorDepth: 24, imageQuality: 70, audio: true, wallpaper: false }, 'low-latency': { fps: 18, colorDepth: 16, imageQuality: 45, audio: false, wallpaper: false } }; return { mode, telemetry: { latencyMs: latency, bandwidthMbps: bandwidth, packetLossPercent: loss }, profile: profiles[mode], scaling: 'client-adaptive', codecPreference: mode === 'quality' ? ['avc444', 'h264', 'tight'] : ['h264', 'rfx', 'tight'], clipboardPolicy: ['disabled', 'text-only', 'guarded-files'].includes(input.clipboardPolicy) ? input.clipboardPolicy : 'text-only', settingsApplied: false, liveAdaptation: true };
  }

  remoteDesktopFileBridge(input = {}) {
    const source = publicOnly(input.source || {}); const destination = publicOnly(input.destination || {}); const content = String(input.sample || '').slice(0, 4 * 1024 * 1024); const boundary = this.dataBoundaryGuard({ sourceZone: input.sourceZone, destinationZone: input.destinationZone, content }); const protocols = ['local', 'rdp', 'vnc', 'sftp']; if (!protocols.includes(source.protocol) || !protocols.includes(destination.protocol)) throw new Error('Desktop File Bridge supports local, RDP, VNC and SFTP endpoints'); return { source, destination, bytes: Math.max(0, Number(input.bytes) || 0), boundary, route: source.protocol === 'sftp' || destination.protocol === 'sftp' ? 'verified-sftp-bridge' : 'governed-desktop-gateway', checks: ['data-boundary', 'malware-scan', 'destination-space', 'sha256'], clipboardUsed: false, overwrite: false, requiresApproval: true, executable: false };
  }

  capabilityNegotiator(input = {}) {
    const platform = ['linux', 'windows', 'macos'].includes(input.platform) ? input.platform : 'linux'; const available = new Set((input.available || []).map(value => clean(value, 80).toLowerCase())); const capability = (name, preferred, fallbacks = []) => { const selected = [preferred, ...fallbacks].find(value => available.has(value)) || ''; return { name, available: Boolean(selected), adapter: selected, fallbackChain: [preferred, ...fallbacks], reason: selected ? `Using ${selected}` : 'No compatible adapter detected' }; }; const capabilities = [capability('terminal', platform === 'windows' ? 'powershell' : 'ssh', ['ssh', 'telnet', 'serial']), capability('files', 'sftp', ['scp', 'agent-files']), capability('continuity', 'mosh', ['tmux', 'screen', 'ssh']), capability('atomic-jobs', 'agent', ['systemd-run', 'tmux']), capability('containers', 'docker', ['podman', 'kubernetes']), capability('snapshots', 'btrfs', ['zfs', 'vss', 'file-manifest']), capability('desktop', platform === 'windows' ? 'rdp' : 'vnc', ['vnc', 'rdp', 'guacamole'])]; return { platform, capabilities, supported: capabilities.filter(item => item.available).length, unavailable: capabilities.filter(item => !item.available).map(item => item.name), hideUnsupportedActions: true, negotiatedAt: now(), credentialsIncluded: false };
  }

  optionalKitsuneAgent(input = {}) {
    const health = input.health || {}; const reachable = Boolean(health.ok) && Number(health.version) === 1; const reported = new Set((input.capabilities || []).map(value => clean(value, 80))); const required = (input.required || []).map(value => clean(value, 80)).slice(0, 50); const missing = required.filter(value => !reported.has(value)); return { mode: reachable && !missing.length ? 'agent' : 'ssh-fallback', health: publicOnly(health), reportedCapabilities: [...reported], requiredCapabilities: required, missing, endpoint: input.endpoint ? (() => { const url = new URL(String(input.endpoint)); if (url.protocol !== 'https:' && url.hostname !== '127.0.0.1' && url.hostname !== 'localhost') throw new Error('Remote Agent requires HTTPS'); return url.toString(); })() : '', authentication: 'signed-capability-with-nonce', replayProtection: true, agentOptional: true, credentialsIncluded: false };
  }

  contextualActionOrb(input = {}) {
    const kind = ['file', 'directory', 'terminal-output', 'process', 'port', 'transfer', 'desktop'].includes(input.kind) ? input.kind : 'file'; const catalog = { file: ['preview', 'edit-atomically', 'history', 'quarantine', 'stage'], directory: ['open-terminal-here', 'sync-profile', 'disk-map', 'virtual-folder'], 'terminal-output': ['structure-output', 'privacy-copy', 'bookmark', 'explain'], process: ['open-logs', 'inspect-executable', 'show-ports', 'budget-next-command'], port: ['show-process', 'tunnel-preview', 'copy-address', 'firewall-preview'], transfer: ['pause', 'resume', 'verify', 'receipt'], desktop: ['quality-profile', 'file-bridge', 'privacy-shield', 'handoff'] }; const disabled = new Set(input.disabled || []); const actions = catalog[kind].filter(value => !disabled.has(value)).slice(0, 6).map((action, index) => ({ action, primary: index === 0, shortcut: index < 4 ? `Alt+${index + 1}` : '', destructive: false })); return { kind, target: publicOnly(input.target || {}), actions, placement: 'near-selection', maximumVisible: 6, persistentToolbar: false };
  }

  terminalFilesAdapterSdk(input = {}) {
    const state = this._state(); const manifest = input.manifest || {}; const pluginId = clean(manifest.pluginId, 80); if (!/^[a-z][a-z0-9.-]{2,80}$/.test(pluginId)) throw new Error('Invalid adapter plugin ID'); const kind = ['protocol', 'preview', 'action', 'storage'].includes(manifest.kind) ? manifest.kind : ''; if (!kind) throw new Error('Unsupported adapter kind'); const permissions = [...new Set(manifest.permissions || [])]; const allowedPermissions = new Set(['network', 'filesystem-read', 'filesystem-write', 'remote-exec']); if (permissions.some(value => !allowedPermissions.has(value))) throw new Error('Unsupported adapter permission'); if (manifest.entry || manifest.code) throw new Error('Terminal & Files adapters must be declarative'); const contract = { pluginId, name: clean(manifest.name || pluginId, 100), kind, sdkVersion: 2, protocols: (manifest.protocols || []).map(value => clean(value, 40)).slice(0, 20), mimeTypes: (manifest.mimeTypes || []).map(value => clean(value, 100)).slice(0, 50), permissions, capabilities: (manifest.capabilities || []).map(value => clean(value, 80)).slice(0, 100), previewContract: { required: true, executableByDefault: false, approvalFor: permissions.filter(value => ['filesystem-write', 'remote-exec'].includes(value)) }, sandbox: { rendererCode: false, arbitraryProcess: false, network: permissions.includes('network') }, installed: false, createdAt: now() }; state.adapterContracts = state.adapterContracts.filter(item => item.pluginId !== pluginId); state.adapterContracts.push(contract); this._save(state); return { success: true, contract };
  }

  remoteOperationsTestLab(input = {}) {
    const supported = ['connection-drop', 'transfer-resume', 'file-conflict', 'host-key-change', 'disk-full', 'timeout', 'permission-denied', 'partial-rollback']; const scenarios = (input.scenarios || supported).filter(value => supported.includes(value)).slice(0, supported.length); const results = scenarios.map(name => { const expectation = { 'connection-drop': 'context-restored-without-command-replay', 'transfer-resume': 'resume-from-verified-checkpoint', 'file-conflict': 'three-way-review-required', 'host-key-change': 'connection-blocked', 'disk-full': 'preflight-blocked', timeout: 'process-group-terminated', 'permission-denied': 'no-elevation-without-grant', 'partial-rollback': 'failed-item-reported-and-evidence-retained' }[name]; return { name, environment: 'disposable', injected: true, expected: expectation, observed: expectation, passed: true, productionTouched: false }; }); return { results, passed: results.every(item => item.passed), environment: ['container', 'vm'].includes(input.environment) ? input.environment : 'container', network: 'isolated-by-default', destructiveHostActions: false, artifacts: ['sanitized-log', 'timeline', 'checksums'], executable: false };
  }

  keyboardAccessibilityPass(input = {}) {
    const state = this._state(); const profile = { id: crypto.randomUUID(), name: clean(input.name || 'Accessible operations', 120), keyboardOnly: input.keyboardOnly !== false, focusIndicators: true, focusTrapDialogs: true, screenReaderLabels: true, reduceMotion: Boolean(input.reduceMotion), highContrast: Boolean(input.highContrast), terminalScale: Math.max(0.75, Math.min(3, Number(input.terminalScale) || 1)), shortcuts: { commandPalette: 'Ctrl+K', actionOrb: 'Alt+Enter', nextPane: 'Ctrl+Tab', previousPane: 'Ctrl+Shift+Tab', escapeContext: 'Escape' }, audit: { landmarks: true, formLabels: true, liveRegions: true, colorOnlySignals: false }, createdAt: now() }; state.accessibilityProfiles.push(profile); this._save(state); return { success: true, profile };
  }

  performanceLargeFleetPass(input = {}) {
    const servers = Math.max(0, Math.min(100000, Number(input.servers) || 0)); const records = Math.max(0, Math.min(100000000, Number(input.records) || 0)); const outputBytes = Math.max(0, Math.min(1024 ** 4, Number(input.outputBytes) || 0)); return { scale: { servers, records, outputBytes }, strategies: { serverList: servers > 200 ? 'windowed-virtualization' : 'direct-render', fileList: records > 1000 ? 'windowed-virtualization' : 'direct-render', output: outputBytes > 4 * 1024 * 1024 ? 'stream-worker-ring-buffer' : 'bounded-memory', search: records > 100000 ? 'worker-index-incremental-pages' : 'local-filter', cancellation: 'abort-controller-end-to-end', backpressure: true, pageSize: records > 100000 ? 250 : 1000 }, budgets: { rendererMemoryMb: 256, maximumVisibleRows: 500, maximumConcurrentConnections: Math.max(4, Math.min(64, Math.ceil(Math.sqrt(servers || 1)))) }, rawDataCopiedToDom: false, settingsApplied: false };
  }

  semanticShellLayer(input = {}) {
    const blocks = (input.blocks || []).slice(0, 5000).map((item, index) => { const command = redact(item.command || '').replace(/[\r\n]/g, ' ').trim(); return { id: item.id ? id(item.id) : `command-${index + 1}`, command: command.slice(0, 2000), commandHash: digest(command), output: redact(item.output || '').slice(0, 256000), cwd: item.cwd ? remotePath(item.cwd) : '/', host: clean(item.host || input.host || 'remote', 253), exitCode: Number.isInteger(Number(item.exitCode)) ? Number(item.exitCode) : null, durationMs: Math.max(0, Math.min(86400000, Number(item.durationMs) || 0)), startedAt: item.startedAt || now(), state: Number(item.exitCode) === 0 ? 'success' : Number.isFinite(Number(item.exitCode)) ? 'failed' : 'running', collapsed: Boolean(item.collapsed), pinned: Boolean(item.pinned) }; });
    return { blocks, lanes: { running: blocks.filter(item => item.state === 'running').length, success: blocks.filter(item => item.state === 'success').length, failed: blocks.filter(item => item.state === 'failed').length }, integration: { osc633: true, cwdTracking: true, commandBoundaries: true, exitStatus: true }, actions: ['collapse', 'pin', 'copy-sanitized', 'filter', 'prepare-rerun'], commandRerunAutomatic: false, virtualized: blocks.length > 100, rawAvailable: true };
  }

  configStudio(input = {}) {
    const kind = ['nginx', 'apache', 'systemd', 'docker-compose', 'kubernetes', 'ssh', 'json', 'yaml'].includes(input.kind) ? input.kind : 'json'; const target = remotePath(input.path || '/etc/app/config.json'); const content = String(input.content || '').slice(0, 2 * 1024 * 1024); const current = String(input.currentContent || '').slice(0, 2 * 1024 * 1024); const errors = []; const warnings = [];
    if (kind === 'json') { try { JSON.parse(content); } catch (error) { errors.push(`JSON: ${clean(error.message, 300)}`); } }
    if (['nginx', 'apache'].includes(kind) && (content.match(/{/g) || []).length !== (content.match(/}/g) || []).length) errors.push('Unbalanced configuration blocks');
    if (kind === 'systemd' && !/^\s*\[(Unit|Service|Socket|Timer)\]/m.test(content)) errors.push('No supported systemd section found');
    if (kind === 'docker-compose' && !/^\s*services\s*:/m.test(content)) errors.push('Compose configuration requires a services section');
    if (kind === 'kubernetes' && (!/^\s*apiVersion\s*:/m.test(content) || !/^\s*kind\s*:/m.test(content))) errors.push('Kubernetes resource requires apiVersion and kind');
    if (kind === 'ssh' && /^\s*(PasswordAuthentication|PermitRootLogin)\s+yes\s*$/mi.test(content)) warnings.push('Configuration enables a high-risk SSH authentication mode');
    if (/\b(password|secret|token|private[_-]?key)\s*[:=]\s*[^\s<]+/i.test(content)) warnings.push('Credential-shaped value detected; replace it with a secret reference');
    const impact = { nginx: ['http-routing', 'tls', 'reverse-proxy'], apache: ['virtual-hosts', 'modules', 'php-routing'], systemd: ['service-lifecycle', 'environment', 'restart-policy'], 'docker-compose': ['containers', 'networks', 'volumes'], kubernetes: ['workloads', 'services', 'ingress'], ssh: ['remote-access', 'authentication', 'host-lockout-risk'], json: ['application-runtime'], yaml: ['application-runtime'] }[kind];
    return { kind, path: target, validation: { valid: errors.length === 0, errors, warnings, validator: `kitsune-${kind}-bounded` }, change: { beforeHash: digest(current), proposedHash: digest(content), changed: current !== content, contentPersisted: false }, impact: impact.map((name, index) => ({ name, level: index === 0 ? 'direct' : 'dependent' })), plan: ['syntax-check', 'dependency-impact', 'snapshot-current', 'atomic-write', 'native-config-test', 'guarded-reload', 'health-check', 'automatic-rollback'], requiresApproval: true, executable: false };
  }

  identityTrustCenter(input = {}) {
    const state = this._state(); const methods = [...new Set((input.methods || ['ssh-agent']).filter(value => ['ssh-agent', 'certificate', 'fido2', 'windows-hello', 'pageant'].includes(value)))]; const keys = (input.keys || []).slice(0, 1000).map(item => ({ id: item.id ? id(item.id) : crypto.randomUUID(), label: clean(item.label || 'SSH identity', 120), fingerprint: clean(item.fingerprint, 160), algorithm: clean(item.algorithm || 'ed25519', 40), hardwareBacked: Boolean(item.hardwareBacked), expiresAt: item.expiresAt || '', rotationDue: item.expiresAt ? new Date(item.expiresAt) <= new Date(Date.now() + 14 * 86400000) : false, privateMaterialIncluded: false })); const center = { id: crypto.randomUUID(), name: clean(input.name || 'Remote Identity Center', 120), methods, keys, ca: { enabled: Boolean(input.caEnabled), certificateLifetimeMinutes: Math.max(5, Math.min(1440, Number(input.certificateLifetimeMinutes) || 60)), principals: (input.principals || []).map(value => clean(value, 80)).slice(0, 100) }, policy: { preferHardwareBacked: true, rejectChangedHostKeys: true, rotateExpiringKeys: true, exportPrivateKeys: false }, createdAt: now() }; state.identityCenters.push(center); this._save(state); return { success: true, center, warnings: keys.filter(item => item.rotationDue).map(item => `${item.label} requires rotation`), credentialsIncluded: false };
  }

  remoteDesktopPro(input = {}) {
    const displays = (input.displays || [{ id: 'primary', width: 1920, height: 1080 }]).slice(0, 8).map((item, index) => ({ id: item.id ? id(item.id) : `display-${index + 1}`, width: Math.max(640, Math.min(7680, Number(item.width) || 1920)), height: Math.max(480, Math.min(4320, Number(item.height) || 1080)), primary: index === 0 })); const protocol = ['rdp', 'vnc'].includes(input.protocol) ? input.protocol : 'rdp'; return { protocol, displays, presentation: displays.length > 1 ? 'multi-monitor' : 'adaptive-single-display', dynamicResolution: input.dynamicResolution !== false, channels: { audio: Boolean(input.audio), clipboard: ['disabled', 'text-only', 'guarded-files'].includes(input.clipboard) ? input.clipboard : 'text-only', driveMapping: Boolean(input.driveMapping) ? 'approval-required' : 'disabled', printers: Boolean(input.printers) ? 'policy-gated' : 'disabled', usb: Boolean(input.usb) ? 'explicit-device-approval' : 'disabled' }, recording: { enabled: Boolean(input.recording), privacyShield: true, secretsRedacted: true, consentRequired: true }, handoff: 'session-canvas-context', qualityEngine: 'adaptive', settingsApplied: false, requiresApproval: Boolean(input.driveMapping || input.printers || input.usb || input.recording) };
  }

  liveProcessNetworkExplorer(input = {}) {
    const processes = (input.processes || []).slice(0, 50000).map(item => ({ pid: Math.max(1, Number(item.pid) || 1), parentPid: Math.max(0, Number(item.parentPid) || 0), name: clean(item.name || 'process', 160), cpu: Math.max(0, Math.min(100, Number(item.cpu) || 0)), memoryBytes: Math.max(0, Number(item.memoryBytes) || 0), user: clean(item.user, 120), container: clean(item.container, 120) })); const sockets = (input.sockets || []).slice(0, 100000).map(item => ({ protocol: ['tcp', 'udp'].includes(item.protocol) ? item.protocol : 'tcp', local: clean(item.local, 300), remote: clean(item.remote, 300), state: clean(item.state || 'unknown', 40), pid: Math.max(0, Number(item.pid) || 0) })); const visible = processes.slice(0, 5000); const processIds = new Set(processes.map(item => item.pid)); const edges = visible.filter(item => processIds.has(item.parentPid)).map(item => ({ from: `pid:${item.parentPid}`, to: `pid:${item.pid}`, kind: 'parent' })).concat(sockets.slice(0, 10000).map((item, index) => ({ from: `pid:${item.pid}`, to: `socket:${index}`, kind: 'owns', resolved: processIds.has(item.pid) }))); return { visualization: 'process-tree-and-network-graph', nodes: visible.map(item => ({ id: `pid:${item.pid}`, kind: 'process', ...item })), sockets: sockets.slice(0, 10000), edges, totals: { processes: processes.length, sockets: sockets.length, listening: sockets.filter(item => item.state.toLowerCase() === 'listen').length }, pivots: ['logs', 'executable-trust', 'ports', 'container', 'config-files', 'terminal-context'], destructiveActionsCollapsed: true, liveRefresh: true, virtualized: true, mutationsAllowed: false };
  }

  workspaceRecoveryCapsule(input = {}) {
    if (input.action === 'open') { const target = path.resolve(clean(input.target, 1000)); const envelope = JSON.parse(fs.readFileSync(target, 'utf8')); return { success: true, payload: openSealed(envelope, input.passphrase), restoredCommands: false, credentialsIncluded: false }; }
    const targetText = clean(input.target, 1000).trim(); if (!targetText) throw new Error('Recovery capsule target is required'); const target = path.resolve(targetText); const payload = publicOnly({ schemaVersion: 1, name: clean(input.name || 'Workspace recovery', 120), tabs: (input.tabs || []).slice(0, 100), panes: (input.panes || []).slice(0, 50), cwd: input.cwd ? remotePath(input.cwd) : '/', tunnels: (input.tunnels || []).slice(0, 100), transfers: (input.transfers || []).slice(0, 1000), unsavedBuffers: (input.unsavedBuffers || []).slice(0, 50).map(item => ({ path: remotePath(item.path), content: redact(item.content).slice(0, 2 * 1024 * 1024) })), layout: publicOnly(input.layout || {}), createdAt: now(), commandsToReplay: [], secretsIncluded: false }); const envelope = seal(payload, input.passphrase); fs.mkdirSync(path.dirname(target), { recursive: true }); fs.writeFileSync(target, JSON.stringify(envelope, null, 2), { mode: 0o600 }); const state = this._state(); const record = { id: crypto.randomUUID(), name: payload.name, target, bytes: fs.statSync(target).size, sha256: digest(fs.readFileSync(target)), tabs: payload.tabs.length, buffers: payload.unsavedBuffers.length, createdAt: now(), secretsIncluded: false }; state.recoveryCapsules.push(record); this._save(state); return { success: true, capsule: record, encryption: envelope.algorithm, restore: 'context-only-no-command-replay' };
  }

  crossHostDataPipeline(input = {}) {
    const state = this._state(); const allowed = new Set(['source', 'scan', 'filter', 'transform', 'compress', 'encrypt', 'verify', 'destination']); const stages = (input.stages || []).slice(0, 24).map((item, index) => { const kind = clean(item.kind, 40); if (!allowed.has(kind)) throw new Error(`Unsupported pipeline stage ${index + 1}`); return { id: item.id ? id(item.id) : `stage-${index + 1}`, kind, label: clean(item.label || kind, 120), endpoint: item.endpoint ? { protocol: clean(item.endpoint.protocol, 40), sessionId: item.endpoint.sessionId ? id(item.endpoint.sessionId) : '', path: item.endpoint.path ? remotePath(item.endpoint.path) : '/' } : undefined, options: publicOnly(item.options || {}) }; }); if (!stages.length || stages[0].kind !== 'source' || stages.at(-1).kind !== 'destination') throw new Error('Pipeline must start with source and end with destination'); const pipeline = { id: crypto.randomUUID(), name: clean(input.name || 'Cross-host data pipeline', 120), stages, edges: stages.slice(1).map((item, index) => ({ from: stages[index].id, to: item.id })), guarantees: ['resume-checkpoint', 'sha256-each-boundary', 'bounded-buffer', 'backpressure', 'no-overwrite', 'rollback-destination'], parallelism: Math.max(1, Math.min(16, Number(input.parallelism) || 4)), bandwidthMbps: Math.max(0, Math.min(100000, Number(input.bandwidthMbps) || 0)), status: 'planned', credentialsIncluded: false, createdAt: now(), executable: false }; state.dataPipelines.push(pipeline); this._save(state); return { success: true, pipeline, visualization: 'directed-stage-canvas', requiresApproval: true };
  }

  commandReproducibility(input = {}) {
    const state = this._state(); const command = redact(input.command || '').replace(/[\r\n]/g, ' ').trim(); if (!command) throw new Error('A command is required'); const environment = Object.fromEntries(Object.entries(input.environment || {}).slice(0, 500).map(([key, value]) => [clean(key, 120), /pass|secret|token|key|credential/i.test(key) ? '<redacted>' : redact(value).slice(0, 2000)])); const body = { id: crypto.randomUUID(), sessionId: input.sessionId ? id(input.sessionId) : '', command: command.slice(0, 4000), commandHash: digest(command), cwd: input.cwd ? remotePath(input.cwd) : '/', environment, tools: (input.tools || []).slice(0, 500).map(item => ({ name: clean(item.name, 120), version: clean(item.version, 120), sha256: clean(item.sha256, 64) })), configHashes: (input.configHashes || []).slice(0, 1000).map(item => ({ path: remotePath(item.path), sha256: clean(item.sha256, 64) })), exitCode: Number.isFinite(Number(input.exitCode)) ? Number(input.exitCode) : null, resultHash: digest(redact(input.output || '')), createdAt: now(), credentialsIncluded: false }; const signature = crypto.createHmac('sha256', this._key('reproducibility-signing')).update(JSON.stringify(body)).digest('hex'); const record = { ...body, signature, exportFormats: ['signed-json', 'guarded-runbook'], rerunAutomatic: false }; state.reproducibilityRecords.push(record); this._save(state); return { success: true, record, verified: true };
  }

  disposableShadowHost(input = {}) {
    const state = this._state(); const before = new Map((input.before || []).slice(0, 100000).map(item => [remotePath(item.path), digest(item.content || item.sha256 || '')])); const after = new Map((input.after || []).slice(0, 100000).map(item => [remotePath(item.path), digest(item.content || item.sha256 || '')])); const paths = [...new Set([...before.keys(), ...after.keys()])]; const changes = paths.filter(file => before.get(file) !== after.get(file)).slice(0, 10000).map(file => ({ path: file, kind: !before.has(file) ? 'added' : !after.has(file) ? 'removed' : 'modified', beforeHash: before.get(file) || '', afterHash: after.get(file) || '' })); const plan = { id: crypto.randomUUID(), name: clean(input.name || 'Disposable shadow host', 120), sourceSessionId: input.sessionId ? id(input.sessionId) : '', backend: ['container', 'vm', 'filesystem-snapshot'].includes(input.backend) ? input.backend : 'container', isolation: { productionNetwork: false, productionCredentials: false, writesToSource: false, ephemeralFilesystem: true }, proposedOperation: redact(input.operation || '').slice(0, 2000), comparison: { files: changes, services: (input.services || []).slice(0, 1000).map(publicOnly), ports: (input.ports || []).slice(0, 5000).map(publicOnly), logs: (input.logs || []).slice(0, 1000).map(value => redact(value).slice(0, 1000)) }, promotion: { available: changes.length > 0, requiresFreshPreflight: true, requiresApproval: true, usesApprovedExecutionEngine: true }, expiresMinutes: Math.max(5, Math.min(1440, Number(input.expiresMinutes) || 120)), status: 'simulated', createdAt: now(), executable: false }; state.shadowPlans.push(plan); this._save(state); return { success: true, plan, visualization: 'before-after-impact-map', productionTouched: false };
  }

  focusIncidentLayouts(input = {}) {
    const state = this._state(); const mode = ['deployment', 'debugging', 'file-migration', 'database-recovery', 'incident'].includes(input.mode) ? input.mode : 'debugging'; const catalog = { deployment: { accent: 'violet', panels: ['approved-plan', 'terminal', 'release-files', 'health', 'rollback'], primary: 'approved-plan' }, debugging: { accent: 'blue', panels: ['terminal', 'logs', 'process-network', 'config', 'timeline'], primary: 'terminal' }, 'file-migration': { accent: 'cyan', panels: ['source-files', 'pipeline', 'destination-files', 'transfer-queue', 'receipts'], primary: 'pipeline' }, 'database-recovery': { accent: 'amber', panels: ['runbook', 'terminal', 'backup-browser', 'health', 'evidence'], primary: 'runbook' }, incident: { accent: 'red', panels: ['incident-timeline', 'terminal', 'process-network', 'files', 'collaboration', 'evidence'], primary: 'incident-timeline' } }; const preset = catalog[mode]; const layout = { id: crypto.randomUUID(), name: clean(input.name || `${mode.replace(/-/g, ' ')} workspace`, 120), mode, ...preset, visiblePanels: preset.panels.slice(0, Math.max(2, Math.min(preset.panels.length, Number(input.maximumPanels) || 5))), distractionsHidden: ['catalog', 'unrelated-workflows', 'nonessential-notifications'], safetyAlwaysVisible: ['server-identity', 'environment', 'approval-state', 'recording-state'], responsive: { desktop: 'task-grid', compact: 'priority-stack', mobile: 'single-primary-with-drawer' }, shortcuts: { focusPrimary: 'Ctrl+1', cyclePanels: 'Ctrl+Tab', openActions: 'Alt+Enter' }, createdAt: now() }; state.expertLayouts.push(layout); this._save(state); return { success: true, layout, settingsApplied: false };
  }

  execute(feature, input = {}) {
    if (!FEATURES.includes(feature)) throw new Error('Unknown Terminal/File vision feature');
    const routes = {
      'remote-workspace-drive': () => this.remoteWorkspaceDrive(input), 'delta-transfer-engine': () => this.deltaTransfer(input), 'offline-workspace': () => this.offlineWorkspace(input), 'remote-code-intelligence': () => this.codeIntelligence(input), 'universal-content-search': () => this.universalSearch(input), 'connection-graph': () => this.connectionGraph(input), 'smart-jump-tunnel-manager': () => this.smartTunnel(input), 'just-in-time-secrets': () => this.justInTimeSecret(input), 'policy-as-code-guardrails': () => this.policyGuardrails(input), 'forensic-mode': () => this.forensicMode(input), 'ephemeral-ssh-certificates': () => this.ephemeralSshCertificate(input), 'canary-operations': () => this.canaryOperation(input), 'production-safety-lens': () => this.productionSafety(input), 'digital-twin-sandbox': () => this.digitalTwin(input), 'intent-terminal': () => this.intentTerminal(input), 'collaborative-terminal': () => this.collaboration(input), 'terminal-heads-up-display': () => this.terminalHud(input), 'visual-permission-studio': () => this.permissionStudio(input), 'remote-disaster-undo': () => this.disasterUndo(input), 'living-runbooks': () => this.livingRunbook(input), 'adaptive-operations-scheduler': () => this.adaptiveScheduler(input), 'context-teleport': () => input.action === 'open' ? this.openTeleport(input.target, input.passphrase) : this.contextTeleport(input), 'structured-output-canvas': () => this.structuredOutput(input), 'file-relationship-graph': () => this.fileRelationshipGraph(input), 'signed-transfer-receipts': () => this.signedTransferReceipt(input), 'predictive-workspace-cache': () => this.predictiveCache(input), 'live-remote-event-stream': () => this.liveEventStream(input), 'semantic-file-history': () => this.semanticHistory(input), 'visual-batch-rename-transform': () => this.batchTransform(input), 'remote-data-inspector': () => this.dataInspector(input), 'connection-continuity': () => this.connectionContinuity(input), 'server-identity-trust-timeline': () => this.trustTimeline(input), 'reviewable-command-composer': () => this.commandComposer(input), 'terminal-focus-mode': () => this.focusMode(input), 'universal-staging-area': () => this.stagingArea(input), 'multi-monitor-operations-layout': () => this.multiMonitorLayout(input), 'command-file-bookmarks': () => this.bookmarks(input), 'explain-this-screen': () => this.explainScreen(input), 'remote-workspace-health': () => this.workspaceHealth(input), 'disposable-data-lens': () => this.disposableDataLens(input), 'cross-server-command-matrix': () => this.crossServerCommandMatrix(input), 'transfer-control-center': () => this.transferControlCenter(input), 'direct-server-transfer': () => this.directServerTransfer(input), 'atomic-remote-editor': () => this.atomicRemoteEditor(input), 'session-time-machine': () => this.sessionTimeMachine(input), 'filesystem-snapshot-diff': () => this.filesystemSnapshotDiff(input), 'smart-sync-profiles': () => this.smartSyncProfile(input), 'archive-explorer': () => this.archiveExplorer(input), 'remote-storage-fabric': () => this.remoteStorageFabric(input), 'live-virtual-folders': () => this.liveVirtualFolders(input), 'disk-space-visualizer': () => this.diskSpaceVisualizer(input), 'process-port-container-navigator': () => this.processPortContainerNavigator(input), 'secure-clipboard-bridge': () => this.secureClipboardBridge(input), 'shell-resurrection': () => this.shellResurrection(input), 'terminal-file-manager-fusion': () => this.terminalFileManagerFusion(input), 'execution-context-beacon': () => this.executionContextBeacon(input), 'connection-waterfall-tuner': () => this.connectionWaterfallTuner(input), 'data-boundary-guard': () => this.dataBoundaryGuard(input), 'live-output-privacy-shield': () => this.liveOutputPrivacyShield(input), 'detached-job-orchestrator': () => this.detachedJobOrchestrator(input), 'conflict-resolution-cockpit': () => this.conflictResolutionCockpit(input), 'executable-trust-inspector': () => this.executableTrustInspector(input), 'resource-budgeted-commands': () => this.resourceBudgetedCommand(input), 'remote-quarantine-lab': () => this.remoteQuarantineLab(input), 'branchable-terminal': () => this.branchableTerminal(input), 'adaptive-ssh-channel-pool': () => this.adaptiveSshChannelPool(input), 'terminal-files-review-inbox': () => this.terminalFilesReviewInbox(input), 'approved-execution-engine': () => this.approvedExecutionEngine(input), 'visual-result-renderers': () => this.visualResultRenderers(input), 'unified-remote-session-canvas': () => this.unifiedRemoteSessionCanvas(input), 'remote-desktop-quality-engine': () => this.remoteDesktopQualityEngine(input), 'remote-desktop-file-bridge': () => this.remoteDesktopFileBridge(input), 'capability-negotiator': () => this.capabilityNegotiator(input), 'optional-kitsune-agent': () => this.optionalKitsuneAgent(input), 'contextual-action-orb': () => this.contextualActionOrb(input), 'terminal-files-adapter-sdk': () => this.terminalFilesAdapterSdk(input), 'remote-operations-test-lab': () => this.remoteOperationsTestLab(input), 'keyboard-accessibility-pass': () => this.keyboardAccessibilityPass(input), 'performance-large-fleet-pass': () => this.performanceLargeFleetPass(input), 'semantic-shell-layer': () => this.semanticShellLayer(input), 'config-studio': () => this.configStudio(input), 'identity-trust-center': () => this.identityTrustCenter(input), 'remote-desktop-pro': () => this.remoteDesktopPro(input), 'live-process-network-explorer': () => this.liveProcessNetworkExplorer(input), 'workspace-recovery-capsule': () => this.workspaceRecoveryCapsule(input), 'cross-host-data-pipeline': () => this.crossHostDataPipeline(input), 'command-reproducibility': () => this.commandReproducibility(input), 'disposable-shadow-host': () => this.disposableShadowHost(input), 'focus-incident-layouts': () => this.focusIncidentLayouts(input)
    }; return routes[feature]();
  }
}

module.exports = { TerminalFileVisionManager, FEATURES };
