name: Protocol certification
on:
  workflow_dispatch:
  pull_request:
    paths: ['src/remote-access-manager.js', 'src/terminal-file-**', 'scripts/protocol-certification.js', '.github/workflows/protocol-certification.yml']
jobs:
  client-matrix:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: npm }
      - run: npm ci
      - run: node scripts/protocol-certification.js --plan
      - run: npm test
