'use strict';
const test = require('node:test'); const assert = require('node:assert/strict'); const RemoteDevOpsManager = require('../src/remote-devops-manager');
test('DevOps commands constrain paths, actions and identifiers', async () => { const calls = []; const manager = new RemoteDevOpsManager({ exec: async (_input, command, timeout) => { calls.push({ command, timeout }); return { success: true, stdout: '' }; } }); await manager.git({}, '/srv/app', 'status'); await manager.compose({}, '/srv/app', 'logs', 'web'); await manager.kubernetes({}, 'pods', { namespace: 'prod' }); assert.match(calls[0].command, /git status/); assert.match(calls[1].command, /docker compose logs/); assert.match(calls[2].command, /kubectl get pods/); assert.throws(() => manager.git({}, '../etc', 'status'), /Invalid/); assert.throws(() => manager.compose({}, '/srv/app', 'destroy'), /Unsupported/); });
